# Исправление пароля базы данных

## Проблема
```
password authentication failed for user "postgres"
```

Пароль в файле `.env.local` не совпадает с реальным паролем PostgreSQL.

## Решение

### Способ 1: Автоматический поиск пароля

Запустите:
```bash
find-password.bat
```

Скрипт попробует стандартные пароли:
- postgres
- admin
- root
- 12345
- password
- 123456
- postgres123

Если найдет правильный, покажет его.

### Способ 2: Ручное изменение пароля

#### Шаг 1: Откройте файл .env.local

Найдите строку:
```
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/investpro
```

#### Шаг 2: Замените ВАШ_ПАРОЛЬ

Попробуйте эти пароли по очереди:

**Вариант А:**
```
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/investpro
```

**Вариант Б:**
```
DATABASE_URL=postgresql://postgres:admin@localhost:5432/investpro
```

**Вариант В:**
```
DATABASE_URL=postgresql://postgres:root@localhost:5432/investpro
```

**Вариант Г:**
```
DATABASE_URL=postgresql://postgres:12345@localhost:5432/investpro
```

#### Шаг 3: Перезапустите сервер

После изменения пароля:
1. Нажмите `Ctrl+C` в терминале
2. Запустите: `npm run dev`

#### Шаг 4: Попробуйте зарегистрироваться

Откройте http://localhost:3000/register и попробуйте снова.

### Способ 3: Сброс пароля PostgreSQL

Если не помните пароль, сбросьте его:

#### Через pgAdmin:
1. Откройте pgAdmin
2. Подключитесь к серверу (если попросит пароль, попробуйте стандартные)
3. Правый клик на "postgres" (пользователь) → Properties
4. Вкладка "Definition"
5. Введите новый пароль (например, `postgres`)
6. Нажмите "Save"

#### Через командную строку:
```bash
# Войдите как суперпользователь Windows
psql -U postgres

# В консоли PostgreSQL:
ALTER USER postgres WITH PASSWORD 'postgres';
\q
```

### Способ 4: Использование другого пользователя

Если у вас есть другой пользователь PostgreSQL:

```
DATABASE_URL=postgresql://ИМЯ_ПОЛЬЗОВАТЕЛЯ:ПАРОЛЬ@localhost:5432/investpro
```

## Проверка подключения

После изменения пароля проверьте:

```bash
test-connection.bat
```

Должно показать:
```
✓ Подключение успешно!
```

## Частые ошибки

### "FATAL: database "investpro" does not exist"

База данных не создана. Создайте её:

```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу
CREATE DATABASE investpro;
\q
```

Или через pgAdmin:
1. Правый клик на "Databases" → Create → Database
2. Имя: `investpro`
3. Save

### "psql: command not found"

PostgreSQL не добавлен в PATH. Используйте полный путь:

```bash
"C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres
```

(Замените 15 на вашу версию PostgreSQL)

## Итоговая проверка

После исправления пароля:

1. ✅ Файл .env.local обновлен с правильным паролем
2. ✅ Сервер перезапущен (`npm run dev`)
3. ✅ test-connection.bat показывает успех
4. ✅ Регистрация работает!

## Пример правильного .env.local

```env
# База данных PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/investpro

# Альтернативные переменные для базы данных
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/investpro
POSTGRES_PRISMA_URL=postgresql://postgres:postgres@localhost:5432/investpro
```

После исправления пароля регистрация заработает! 🎉
