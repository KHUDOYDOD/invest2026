# 🔧 РЕШЕНИЕ ОШИБКИ 401 (Unauthorized)

## Проблема
API возвращает 401 - "Не авторизован"

## Причина
Токен в localStorage был создан до того, как пользователь получил роль admin. Токен содержит старую информацию о роли.

## Решение

### Вариант 1: Выйти и войти заново (РЕКОМЕНДУЕТСЯ)
1. Нажмите кнопку "Выйти" в правом верхнем углу
2. Войдите снова с теми же данными:
   - Email: `123456@123456.123456`
   - Пароль: ваш пароль
3. Новый токен будет содержать роль admin
4. Обновите страницу админ-панели

### Вариант 2: Очистить localStorage вручную
1. Откройте консоль браузера (F12)
2. Перейдите на вкладку "Application" (Chrome) или "Storage" (Firefox)
3. Найдите "Local Storage" → `http://localhost:3000`
4. Удалите ключи:
   - `authToken`
   - `userId`
5. Обновите страницу
6. Войдите снова

### Вариант 3: Через консоль браузера
1. Откройте консоль (F12)
2. Выполните:
   ```javascript
   localStorage.clear()
   location.reload()
   ```
3. Войдите снова

## Проверка

После входа проверьте в консоли браузера:

```javascript
// Проверить наличие токена
console.log('Token:', localStorage.getItem('authToken') ? 'present' : 'missing')
console.log('User ID:', localStorage.getItem('userId'))

// Тест API
fetch('/api/admin/deposit-requests', {
  headers: { 
    'Authorization': 'Bearer ' + localStorage.getItem('authToken') 
  }
})
.then(r => r.json())
.then(d => console.log('API Response:', d))
```

Должен вернуться ответ с заявками, а не ошибка 401.

## Альтернатива: Войти как salamoni@salamoni.salamoni

Этот пользователь уже имеет роль admin:
1. Выйдите из текущего аккаунта
2. Войдите как:
   - Email: `salamoni@salamoni.salamoni`
   - Пароль: ваш пароль
3. Откройте админ-панель `/admin/requests`
4. Должны отображаться 4 заявки

## Что проверить после входа

### 1. Страница "Мои заявки" (`/dashboard/requests`)
- Должны отображаться заявки пользователя
- Если вы вошли как `salamoni@salamoni.salamoni`, должно быть 4 заявки

### 2. Админ-панель (`/admin/requests`)
- Должны отображаться ВСЕ заявки всех пользователей
- Должны быть кнопки "Одобрить" и "Отклонить"

### 3. Консоль браузера
Должны быть логи:
```
🔄 Fetching admin requests...
Deposit response status: 200
✅ Deposit requests loaded: 4
```

### 4. Консоль сервера
Должны быть логи:
```
Admin API - Auth header: present
Admin API - Token extracted, length: XXX
Admin API - Token verified for user: 1
Admin loading deposit requests...
Found 4 deposit requests
```

## Если все еще не работает

1. **Перезапустите сервер:**
   ```bash
   # Остановите текущий процесс (Ctrl+C)
   npm run dev
   ```

2. **Проверьте переменные окружения:**
   ```bash
   # Убедитесь, что .env.local существует
   type .env.local
   ```

3. **Проверьте базу данных:**
   ```bash
   node check-user-role.js
   node check-deposit-requests.js
   ```

4. **Откройте тестовую страницу:**
   - Перейдите на `http://localhost:3000/test-api-auth.html`
   - Нажмите "Check LocalStorage"
   - Нажмите "Test Admin API"
   - Посмотрите результаты

## Готово! ✅

После выхода и повторного входа все должно работать!
