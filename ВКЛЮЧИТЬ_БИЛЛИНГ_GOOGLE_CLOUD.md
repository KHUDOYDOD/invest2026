# 💳 Как включить биллинг в Google Cloud

## ⚠️ Без биллинга Cloud Run не работает!

Но не волнуйтесь - Google дает **$300 бесплатных кредитов на 90 дней**!

---

## 📋 Шаг 1: Откройте страницу биллинга

1. Перейдите: https://console.cloud.google.com/billing
2. Или в Cloud Console нажмите меню (☰) → **Billing**

---

## 📋 Шаг 2: Создайте биллинг аккаунт

1. Нажмите **"Create Account"** или **"Manage Billing Accounts"**
2. Нажмите **"Add Billing Account"**
3. Выберите страну
4. Примите условия

---

## 💳 Шаг 3: Добавьте карту

Google попросит добавить карту для:
- ✅ Подтверждения личности
- ✅ Получения $300 бесплатных кредитов

**Важно:**
- Деньги НЕ спишутся автоматически
- Вы получите $300 кредитов на 90 дней
- После окончания кредитов нужно вручную включить платную версию

---

## 📋 Шаг 4: Привяжите проект к биллингу

1. Выберите ваш проект: **planar-flux-484404-v7**
2. Нажмите **"Link a Billing Account"**
3. Выберите созданный биллинг аккаунт
4. Нажмите **"Set Account"**

---

## ✅ Шаг 5: Проверьте, что биллинг включен

В Cloud Shell выполните:

```bash
gcloud beta billing projects describe planar-flux-484404-v7
```

Должно показать: `billingEnabled: true`

---

## 🚀 Шаг 6: Теперь можно деплоить!

После включения биллинга выполните в Cloud Shell:

```bash
cd ~/invest2026

# Включите API (теперь должно работать)
gcloud services enable sqladmin.googleapis.com run.googleapis.com cloudbuild.googleapis.com

# Создайте базу данных
gcloud sql instances create invest2026-db \
  --database-version=POSTGRES_15 \
  --tier=db-f1-micro \
  --region=us-central1 \
  --root-password=MySecurePassword123!

# Создайте базу
gcloud sql databases create invest2026 --instance=invest2026-db

# Задеплойте приложение
gcloud run deploy invest2026-app \
  --source . \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated \
  --set-env-vars "DATABASE_URL=postgresql://postgres:MySecurePassword123!@/invest2026?host=/cloudsql/$(gcloud sql instances describe invest2026-db --format='value(connectionName)')" \
  --set-env-vars "NEXTAUTH_SECRET=super-secret-key-change-this-min-32-chars-long" \
  --add-cloudsql-instances $(gcloud sql instances describe invest2026-db --format='value(connectionName)') \
  --memory 512Mi
```

---

## 💰 Стоимость (после $300 кредитов):

- **Cloud Run:** ~$0-5/месяц (бесплатный уровень покрывает малую нагрузку)
- **Cloud SQL (db-f1-micro):** ~$7-10/месяц
- **Итого:** ~$7-15/месяц

---

## 🆘 Альтернатива БЕЗ карты:

Если не хотите добавлять карту, используйте:

### 1. **Render.com** (бесплатный план)
```bash
# Зарегистрируйтесь на render.com
# Подключите GitHub репозиторий
# Выберите "Web Service"
# Render автоматически задеплоит
```

### 2. **Railway.app** ($5 бесплатных кредитов)
```bash
# Зарегистрируйтесь на railway.app
# Подключите GitHub
# Выберите проект
# Деплой автоматический
```

### 3. **Fly.io** (бесплатный план)
```bash
# Установите flyctl
curl -L https://fly.io/install.sh | sh

# Войдите
flyctl auth login

# Деплой
flyctl launch
```

---

## 🎯 Мой совет:

1. **Если есть карта** → Включите биллинг в Google Cloud (получите $300)
2. **Если нет карты** → Используйте Render.com или Railway.app

Что выбираете? 😊
