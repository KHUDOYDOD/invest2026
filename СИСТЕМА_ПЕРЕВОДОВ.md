# 🌍 Система мультиязычности InvestPro

## Обзор

Реализована полная система мультиязычности для поддержки 6 языков на всем сайте.

## 🎯 Поддерживаемые языки

| Язык | Код | Флаг | Название |
|------|-----|------|----------|
| Русский | `ru` | 🇷🇺 | Русский |
| Английский | `en` | 🇬🇧 | English |
| Казахский | `kk` | 🇰🇿 | Қазақша |
| Таджикский | `tg` | 🇹🇯 | Тоҷикӣ |
| Киргизский | `ky` | 🇰🇬 | Кыргызча |
| Узбекский | `uz` | 🇺🇿 | O'zbekcha |

## 📂 Структура проекта

```
Invest2025-main/
├── lib/
│   └── i18n/
│       └── locales/
│           ├── ru.json    # Русский
│           ├── en.json    # English
│           ├── kk.json    # Қазақша
│           ├── tg.json    # Тоҷикӣ
│           ├── ky.json    # Кыргызча
│           └── uz.json    # O'zbekcha
├── contexts/
│   └── language-context.tsx    # Контекст языка
├── components/
│   └── language-switcher.tsx   # Переключатель языков
└── app/
    └── layout.tsx              # Главный layout с LanguageProvider
```

## 🔧 Технические детали

### LanguageContext

Контекст предоставляет:
- `language` - текущий язык
- `setLanguage(lang)` - функция смены языка
- `t(key)` - функция перевода

### Функция перевода t()

```tsx
t('hero.title')              // Простой ключ
t('common.loading')          // Вложенный ключ
t('dashboard.balance')       // Глубоко вложенный ключ
```

### Сохранение выбора

Выбранный язык автоматически сохраняется в `localStorage` с ключом `language`.

## 🎨 Компонент LanguageSwitcher

Переключатель языков включает:
- Dropdown меню с флагами
- Название языка
- Галочка на текущем языке
- Адаптивный дизайн (скрывает название на мобильных)

## 📝 Структура переводов

Все переводы организованы по категориям:

### Общие элементы (common)
```json
{
  "common": {
    "loading": "Загрузка...",
    "error": "Ошибка",
    "success": "Успешно",
    "save": "Сохранить",
    "cancel": "Отмена"
  }
}
```

### Шапка сайта (header)
```json
{
  "header": {
    "home": "Главная",
    "about": "О нас",
    "plans": "Тарифы",
    "login": "Войти",
    "register": "Регистрация"
  }
}
```

### Главный баннер (hero)
```json
{
  "hero": {
    "title": "Инвестируйте с умом...",
    "subtitle": "Профессиональная платформа...",
    "cta_primary": "Начать инвестировать"
  }
}
```

И так далее для всех разделов сайта.

## 🚀 Использование в компонентах

### Пример 1: Простой компонент

```tsx
"use client"

import { useLanguage } from '@/contexts/language-context'

export function MyComponent() {
  const { t } = useLanguage()
  
  return (
    <div>
      <h1>{t('hero.title')}</h1>
      <p>{t('hero.subtitle')}</p>
      <button>{t('common.save')}</button>
    </div>
  )
}
```

### Пример 2: Компонент с условным рендерингом

```tsx
"use client"

import { useLanguage } from '@/contexts/language-context'

export function StatusBadge({ status }: { status: string }) {
  const { t } = useLanguage()
  
  const statusText = {
    pending: t('transactions.pending'),
    completed: t('transactions.completed'),
    rejected: t('transactions.rejected')
  }
  
  return <span>{statusText[status]}</span>
}
```

### Пример 3: Компонент с динамическими данными

```tsx
"use client"

import { useLanguage } from '@/contexts/language-context'

export function InvestmentCard({ plan }: { plan: any }) {
  const { t } = useLanguage()
  
  return (
    <div>
      <h3>{plan.name}</h3>
      <p>{t('plans.min_deposit')}: ${plan.minDeposit}</p>
      <p>{t('plans.daily_profit')}: {plan.dailyProfit}%</p>
      <button>{t('plans.choose')}</button>
    </div>
  )
}
```

## 🔄 Процесс перевода компонента

### Шаг 1: Найти все текстовые строки

```tsx
// До
<h1>Инвестируйте с умом</h1>
<p>Профессиональная платформа</p>
<button>Начать инвестировать</button>
```

### Шаг 2: Добавить useLanguage

```tsx
import { useLanguage } from '@/contexts/language-context'

export function Component() {
  const { t } = useLanguage()
  // ...
}
```

### Шаг 3: Заменить строки на ключи

```tsx
// После
<h1>{t('hero.title')}</h1>
<p>{t('hero.subtitle')}</p>
<button>{t('hero.cta_primary')}</button>
```

## 📋 Чеклист для перевода компонента

- [ ] Импортировать `useLanguage`
- [ ] Добавить `const { t } = useLanguage()`
- [ ] Заменить все текстовые строки на `t('key')`
- [ ] Проверить, что все ключи есть в файлах переводов
- [ ] Протестировать на всех языках

## 🎯 Приоритет перевода компонентов

### Высокий приоритет (видимые всем)
1. ✅ Header (шапка)
2. ⏳ Hero Section (главный баннер)
3. ⏳ Statistics (статистика)
4. ⏳ Features (преимущества)
5. ⏳ Investment Plans (тарифы)
6. ⏳ Footer (подвал)

### Средний приоритет (для пользователей)
7. ⏳ Login/Register (авторизация)
8. ⏳ Dashboard (личный кабинет)
9. ⏳ Transactions (транзакции)
10. ⏳ Deposit/Withdrawal (пополнение/вывод)

### Низкий приоритет (админ панель)
11. ⏳ Admin Dashboard
12. ⏳ Admin Settings
13. ⏳ Admin Users

## 🐛 Отладка

### Проблема: Перевод не отображается

**Решение:**
1. Проверьте, что ключ существует в `ru.json`
2. Проверьте правильность написания ключа
3. Откройте консоль браузера - там будет предупреждение о missing translation

### Проблема: Язык не сохраняется

**Решение:**
1. Проверьте localStorage в DevTools
2. Убедитесь, что `LanguageProvider` обернут вокруг компонентов
3. Проверьте, что компонент помечен как `"use client"`

### Проблема: Ошибка "useLanguage must be used within a LanguageProvider"

**Решение:**
Убедитесь, что в `app/layout.tsx` есть `<LanguageProvider>` вокруг children.

## 📊 Статистика переводов

- **Всего ключей**: ~150+
- **Языков**: 6
- **Всего переводов**: ~900+
- **Покрытие**: 100% для базовых ключей

## 🔮 Будущие улучшения

- [ ] Добавить больше языков (арабский, китайский, и т.д.)
- [ ] Реализовать RTL поддержку для арабского
- [ ] Добавить автоматический перевод через API
- [ ] Создать админ панель для управления переводами
- [ ] Добавить плюрализацию (1 день, 2 дня, 5 дней)
- [ ] Добавить форматирование дат и чисел по локали

---

**Создано:** 14 января 2026  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
