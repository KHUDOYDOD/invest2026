# ✅ Исправление ошибки в управлении заявками

## Проблема
```
TypeError: request.id.slice is not a function
```

Ошибка возникала при попытке отклонить заявку.

## Причина
В коде использовался метод `.slice()` на `request.id`, но `request.id` это число (integer), а не строка.

```typescript
// ❌ Неправильно
<DialogTitle>Отклонить запрос #{request.id.slice(0, 8)}</DialogTitle>

// ✅ Правильно
<DialogTitle>Отклонить запрос #{request.id}</DialogTitle>
```

## Решение
Убрал `.slice(0, 8)` так как ID это число, а не длинная строка UUID.

## Что было исправлено
- Файл: `app/admin/requests/page.tsx`
- Строка 383: убран `.slice(0, 8)`

## Проверка
1. Запустите сервер: `npm run dev`
2. Откройте: http://localhost:3000/admin/requests
3. Попробуйте отклонить заявку
4. Ошибка больше не должна появляться

## Готово! ✅
Теперь можно:
- ✅ Одобрять заявки
- ✅ Отклонять заявки с причиной
- ✅ Видеть ID заявки в диалоге

Все работает корректно! 🎉
