# 🔄 ПЕРЕУСТАНОВКА POSTGRESQL

## Быстрая переустановка PostgreSQL с новым паролем

### Шаг 1: Удаление старого PostgreSQL

1. **Запустите от имени администратора:** `uninstall-postgresql.bat`
2. Или удалите через "Программы и компоненты" в Windows

### Шаг 2: Установка нового PostgreSQL

1. **Запустите:** `install-postgresql.bat`
2. Скачайте PostgreSQL 16 с официального сайта
3. **Важно:** При установке установите пароль `postgres123`

### Шаг 3: Настройка базы данных

1. **Запустите:** `setup-database-new.bat`
2. Скрипт автоматически:
   - Создаст базу данных `investpro`
   - Инициализирует структуру
   - Обновит `.env.local`
   - Запустит проект

### Шаг 4: Запуск проекта

```bash
npm run dev
```

Откройте: http://localhost:3000

## 🎯 Данные для входа

После настройки используйте:

**Администратор:**
- Email: `admin@example.com`
- Пароль: `X12345x`

**Тестовый пользователь:**
- Email: `test@example.com`
- Пароль: `X12345x`

## ⚡ Альтернативный способ (без переустановки)

Если не хотите переустанавливать PostgreSQL, можете:

1. Открыть pgAdmin (если установлен)
2. Подключиться к серверу
3. Создать новую базу данных `investpro`
4. Импортировать `complete-database-setup.sql`

## 🔧 Если возникли проблемы

1. **PostgreSQL не запускается:**
   - Проверьте службы Windows
   - Запустите `services.msc`
   - Найдите `postgresql-x64-16`
   - Нажмите "Запустить"

2. **Ошибка подключения:**
   - Проверьте пароль в `.env.local`
   - Убедитесь что PostgreSQL запущен
   - Проверьте порт 5432

3. **Ошибка "database does not exist":**
   - Запустите `setup-database-new.bat`
   - Или создайте базу вручную в pgAdmin

## ✅ Готово!

После успешной настройки у вас будет:
- ✅ PostgreSQL с паролем `postgres123`
- ✅ База данных `investpro`
- ✅ Готовая структура таблиц
- ✅ Тестовые данные
- ✅ Работающий проект на http://localhost:3000