# 🔧 Исправление ошибки 502 Bad Gateway

## Проблема
Приложение запущено на VPS, но показывает ошибку 502. Это происходит потому что отсутствуют переменные окружения (DATABASE_URL).

## ✅ Быстрое решение

### Вариант 1: Автоматически (рекомендуется)

Запустите на своем компьютере:
```
vps-fix-502.bat
```

Скрипт автоматически:
- Загрузит файл исправления на VPS
- Создаст правильный `.env.production`
- Перезапустит приложение
- Покажет логи

### Вариант 2: Вручную

1. Подключитесь к VPS:
```
vps-connect.bat
```

2. Когда увидите `root@147805:~#`, выполните:
```bash
cd /root/invest2026

cat > .env.production << 'EOF'
DATABASE_URL=postgresql://neondb_owner:npg_w5yC0HdchuEB@ep-bold-grass-abge4ccn-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345
NODE_ENV=production
NEXT_PUBLIC_API_URL=http://130.49.213.197
EOF

pm2 restart investpro
pm2 logs investpro --lines 20
```

## 🎯 Проверка

После исправления откройте в браузере:
```
http://130.49.213.197
```

Должна открыться главная страница сайта.

## 📊 Полезные команды

Проверить статус:
```bash
pm2 status
```

Посмотреть логи:
```bash
pm2 logs investpro
```

Остановить приложение:
```bash
pm2 stop investpro
```

Запустить снова:
```bash
pm2 start investpro
```

## ❓ Если не работает

1. Проверьте логи: `pm2 logs investpro`
2. Убедитесь что порт 3000 свободен: `netstat -tulpn | grep 3000`
3. Проверьте что Nginx работает: `systemctl status nginx`
