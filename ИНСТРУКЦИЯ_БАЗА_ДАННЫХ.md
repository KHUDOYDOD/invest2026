# 🗄️ ИНСТРУКЦИЯ ПО НАСТРОЙКЕ БАЗЫ ДАННЫХ

## 🎯 ЧТО СДЕЛАНО

Я создал все необходимые файлы для настройки базы данных:

### ✅ Файлы конфигурации:
- `.env.local` - Переменные окружения для подключения к базе данных

### ✅ Скрипты очистки:
- `scripts/clean-demo-data.js` - Автоматическая очистка демо-данных
- `scripts/clean-database.sql` - SQL скрипт для очистки
- `setup-database.bat` - Автоматическая настройка (один клик!)

### ✅ Документация:
- `DATABASE_SETUP.md` - Подробная инструкция
- `QUICK_START.md` - Быстрый старт
- `README_DATABASE.md` - Краткое руководство

## 🚀 КАК ЗАПУСТИТЬ (3 ПРОСТЫХ ШАГА)

### Шаг 1: Установите PostgreSQL

Если еще не установлен:
1. Скачайте: https://www.postgresql.org/download/windows/
2. Установите (запомните пароль!)
3. PostgreSQL автоматически запустится

### Шаг 2: Запустите автоматическую настройку

Просто дважды кликните на файл:
```
setup-database.bat
```

Или в терминале:
```bash
setup-database.bat
```

Скрипт спросит:
1. Пароль PostgreSQL (который вы установили)
2. Хотите ли очистить демо-данные (рекомендуется: да)

### Шаг 3: Запустите сайт

```bash
npm run dev
```

Откройте: http://localhost:3000

## 🔐 СОЗДАНИЕ АДМИНИСТРАТОРА

1. Зарегистрируйтесь на сайте
2. Откройте PowerShell и выполните:

```bash
psql -U postgres -d investpro
```

3. В psql выполните (замените email на свой):

```sql
UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
\q
```

4. Перезайдите на сайт - теперь вы администратор!

## ⚙️ ЧТО НУЖНО НАСТРОИТЬ

### Обязательно: Обновите пароль в .env.local

Откройте файл `.env.local` и замените `password` на ваш реальный пароль PostgreSQL:

```env
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/investpro
```

### Рекомендуется: Измените секретные ключи

Для безопасности сгенерируйте новые ключи:

```powershell
# В PowerShell выполните дважды для двух ключей:
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

Вставьте результаты в `.env.local`:
```env
JWT_SECRET=первый_сгенерированный_ключ
NEXTAUTH_SECRET=второй_сгенерированный_ключ
```

## 🗑️ ОЧИСТКА ДЕМО-ДАННЫХ

### Автоматически (рекомендуется):

```bash
node scripts/clean-demo-data.js
```

Скрипт удалит:
- ❌ Все транзакции
- ❌ Все инвестиции  
- ❌ Все запросы на депозит/вывод
- ❌ Всех демо-пользователей
- ❌ Все уведомления и сообщения
- ❌ Все отзывы и новости

Останется:
- ✅ Чистая структура базы данных
- ✅ Готовая к работе система

### Вручную через SQL:

```bash
psql -U postgres -d investpro -f scripts/clean-database.sql
```

## 🔍 ПРОВЕРКА РАБОТЫ

### Проверка подключения:
```bash
psql -U postgres -d investpro -c "SELECT version();"
```

### Просмотр пользователей:
```bash
psql -U postgres -d investpro -c "SELECT id, email, role FROM users;"
```

### Просмотр статистики:
```bash
psql -U postgres -d investpro -c "SELECT COUNT(*) FROM users;"
```

## 🚨 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Ошибка: "password authentication failed"
**Решение:** Проверьте пароль в `.env.local`

### Ошибка: "database does not exist"
**Решение:** 
```bash
psql -U postgres
CREATE DATABASE investpro;
\q
```

### Ошибка: "psql: command not found"
**Решение:** Добавьте PostgreSQL в PATH:
- Откройте "Переменные среды"
- Добавьте в Path: `C:\Program Files\PostgreSQL\16\bin`

### Ошибка: "connection refused"
**Решение:** Запустите PostgreSQL:
- Откройте "Службы" (services.msc)
- Найдите "postgresql-x64-16"
- Нажмите "Запустить"

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ

### Создание бэкапа:
```bash
pg_dump -U postgres investpro > backup.sql
```

### Восстановление из бэкапа:
```bash
psql -U postgres -d investpro < backup.sql
```

### Просмотр всех таблиц:
```bash
psql -U postgres -d investpro -c "\dt"
```

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

Перед запуском убедитесь:

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `investpro` создана
- [ ] Файл `.env.local` настроен с правильным паролем
- [ ] Демо-данные очищены
- [ ] Создан администратор
- [ ] Сайт запускается: `npm run dev`
- [ ] Можете войти как администратор

## 🎉 ГОТОВО!

Теперь у вас:
- ✅ Настроенная база данных PostgreSQL
- ✅ Чистая система без демо-данных
- ✅ Готовая к работе платформа
- ✅ Доступ администратора

**Следующие шаги:**
1. Войдите как администратор
2. Настройте инвестиционные планы
3. Настройте платежные системы
4. Добавьте контент (новости, отзывы)
5. Начните привлекать пользователей!

## 📞 ДОПОЛНИТЕЛЬНАЯ ПОМОЩЬ

Если нужна помощь:
- Читайте `DATABASE_SETUP.md` - подробная инструкция
- Читайте `QUICK_START.md` - быстрый старт
- Проверьте логи PostgreSQL

---

**Важно:** Перед запуском в продакшене обязательно:
1. Измените все пароли
2. Сгенерируйте новые секретные ключи
3. Включите SSL для базы данных
4. Настройте регулярные бэкапы
5. Ограничьте доступ к базе данных по IP

**Удачи с вашей инвестиционной платформой! 🚀💰**
