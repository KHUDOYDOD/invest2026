# 🚀 Быстрый старт: Мультиязычность

## ✅ Что уже сделано

1. ✅ Создана система переводов для 6 языков (RU, EN, KK, TG, KY, UZ)
2. ✅ Добавлен переключатель языков в Header
3. ✅ Настроен контекст для управления языком
4. ✅ Язык сохраняется в localStorage

## 🎯 Как использовать в компонентах

### Шаг 1: Импортировать хук

```tsx
import { useLanguage } from '@/contexts/language-context'
```

### Шаг 2: Использовать в компоненте

```tsx
export function MyComponent() {
  const { t } = useLanguage()
  
  return (
    <div>
      <h1>{t('hero.title')}</h1>
      <button>{t('common.save')}</button>
    </div>
  )
}
```

### Шаг 3: Заменить текст на ключи

**Было:**
```tsx
<h1>Инвестируйте с умом</h1>
```

**Стало:**
```tsx
<h1>{t('hero.title')}</h1>
```

## 📋 Примеры ключей

```tsx
{t('header.home')}          // Главная
{t('header.login')}         // Войти
{t('header.register')}      // Регистрация
{t('hero.title')}           // Заголовок баннера
{t('hero.subtitle')}        // Подзаголовок
{t('common.loading')}       // Загрузка...
{t('common.save')}          // Сохранить
{t('dashboard.balance')}    // Баланс
{t('plans.title')}          // Инвестиционные планы
```

## 🔍 Где найти все ключи

Откройте файл `lib/i18n/locales/ru.json` - там все ключи с русскими переводами.

## 💡 Совет

Начните с обновления одного компонента, например `hero-section.tsx`, чтобы понять принцип работы.

---

**Готово к использованию!** 🎉
