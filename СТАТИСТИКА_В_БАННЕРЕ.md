# ✅ Статистика в баннере обновлена

## Что изменилось

Теперь **4 карточки статистики в баннере** (hero section) загружают данные из той же базы данных, что и секция "📊 Наши достижения" ниже на странице.

## Откуда берутся данные

Все карточки статистики теперь загружаются из API: `/api/statistics`

Этот API берет данные из таблицы `platform_statistics` в базе данных PostgreSQL.

## Какие данные отображаются

### В баннере (4 карточки):

1. **👥 Активные инвесторы** - количество пользователей (`users_count`)
2. **💰 Общие инвестиции** - сумма инвестиций (`investments_amount`)
3. **📈 Средняя доходность** - процент доходности (`profitability_rate`)
4. **🛡️ Выплачено прибыли** - сумма выплат (`payouts_amount`)

### В анимированных карточках фона:

- Те же данные отображаются в плавающих карточках на фоне баннера

## Как редактировать статистику

Статистику можно редактировать через админ-панель:

1. Войдите как администратор
2. Перейдите в раздел **Статистика** или **Управление сайтом**
3. Измените нужные значения
4. Сохраните изменения

Данные автоматически обновятся на главной странице при следующей загрузке.

## Формат отображения

- Числа форматируются автоматически:
  - `15420` → `15.4K+`
  - `2850000` → `$2.8M`
  - `24.8` → `24.8%`

## Технические детали

- Данные загружаются при монтировании компонента
- Используется React hooks (`useState`, `useEffect`)
- При ошибке загрузки используются значения по умолчанию
- Нет автообновления (данные загружаются один раз при открытии страницы)

---

**Дата обновления:** 14 января 2026
