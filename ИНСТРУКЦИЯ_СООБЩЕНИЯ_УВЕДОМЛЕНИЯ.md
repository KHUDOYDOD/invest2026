# 📬 Инструкция по настройке сообщений и уведомлений

## Что было создано

Я создал полноценную систему сообщений и уведомлений для вашей платформы:

### 1. Страницы
- ✅ `/dashboard/messages` - Страница сообщений пользователя
- ✅ `/dashboard/notifications` - Страница уведомлений пользователя

### 2. API Endpoints
- ✅ `/api/messages` - GET, POST, PATCH, DELETE для сообщений
- ✅ `/api/notifications` - GET, POST, PATCH, DELETE для уведомлений
- ✅ `/api/notifications/preferences` - GET, PUT для настроек уведомлений

### 3. База данных
- ✅ Таблица `messages` - хранение сообщений
- ✅ Таблица `notifications` - хранение уведомлений
- ✅ Таблица `notification_preferences` - настройки уведомлений пользователей

## Установка

### Шаг 1: Создайте таблицы в базе данных

Запустите bat-файл:
```
create-messages-tables.bat
```

Или вручную выполните SQL:
```
psql "%DATABASE_URL%" -f create-messages-notifications-tables.sql
```

### Шаг 2: Проверьте подключение

Убедитесь, что в файле `.env.local` есть переменная:
```
DATABASE_URL=postgresql://username:password@host:port/database
```

### Шаг 3: Перезапустите сервер разработки

```
npm run dev
```

## Функционал

### Страница сообщений (`/dashboard/messages`)

**Возможности:**
- 📥 Просмотр входящих сообщений
- ✉️ Отправка новых сообщений в поддержку
- ✅ Отметка сообщений как прочитанных
- 🗑️ Удаление сообщений
- 💬 Просмотр ответов от администраторов
- 🔍 Поиск по сообщениям

**Вкладки:**
1. **Входящие** - все полученные сообщения
2. **Отправленные** - история отправленных сообщений
3. **Новое** - форма для отправки нового сообщения

### Страница уведомлений (`/dashboard/notifications`)

**Возможности:**
- 🔔 Просмотр всех уведомлений
- ✅ Отметка как прочитанных (одно или все)
- 🗑️ Удаление уведомлений
- ⚙️ Настройка типов уведомлений
- 📊 Статистика по уведомлениям

**Вкладки:**
1. **Все** - все уведомления
2. **Непрочитанные** - только новые
3. **Транзакции** - уведомления о транзакциях
4. **Системные** - системные сообщения
5. **Настройки** - управление типами уведомлений

**Типы уведомлений:**
- ✅ `success` - успешные операции (зеленый)
- ℹ️ `info` - информационные (синий)
- ⚠️ `warning` - предупреждения (желтый)
- ❌ `error` - ошибки (красный)
- 👥 `referral` - реферальные (фиолетовый)
- 🎁 `bonus` - бонусы (оранжевый)
- 💰 `transaction` - транзакции (зеленый)
- ⚙️ `system` - системные (серый)

## Структура базы данных

### Таблица `messages`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (UUID) - ID пользователя
- subject (VARCHAR) - тема сообщения
- message (TEXT) - текст сообщения
- status (VARCHAR) - статус: new, replied, pending, closed
- priority (VARCHAR) - приоритет: low, medium, high
- from_user (VARCHAR) - имя отправителя
- from_email (VARCHAR) - email отправителя
- admin_reply (TEXT) - ответ администратора
- replied_by (UUID) - кто ответил
- replied_at (TIMESTAMP) - когда ответили
- is_read (BOOLEAN) - прочитано ли
- created_at (TIMESTAMP) - дата создания
- updated_at (TIMESTAMP) - дата обновления
```

### Таблица `notifications`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (UUID) - ID пользователя
- type (VARCHAR) - тип уведомления
- title (VARCHAR) - заголовок
- message (TEXT) - текст уведомления
- icon (VARCHAR) - иконка
- color (VARCHAR) - цвет градиента
- is_read (BOOLEAN) - прочитано ли
- action_url (VARCHAR) - ссылка для действия
- metadata (JSONB) - дополнительные данные
- created_at (TIMESTAMP) - дата создания
- read_at (TIMESTAMP) - дата прочтения
```

### Таблица `notification_preferences`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (UUID) - ID пользователя
- email_notifications (BOOLEAN) - email уведомления
- push_notifications (BOOLEAN) - push уведомления
- sms_notifications (BOOLEAN) - SMS уведомления
- deposit_notifications (BOOLEAN) - уведомления о депозитах
- withdrawal_notifications (BOOLEAN) - уведомления о выводах
- referral_notifications (BOOLEAN) - уведомления о рефералах
- system_notifications (BOOLEAN) - системные уведомления
- marketing_notifications (BOOLEAN) - маркетинговые уведомления
- created_at (TIMESTAMP) - дата создания
- updated_at (TIMESTAMP) - дата обновления
```

## Примеры использования API

### Получить сообщения пользователя
```javascript
const response = await fetch("/api/messages", {
  headers: {
    "x-user-id": userId,
  },
})
```

### Отправить новое сообщение
```javascript
const response = await fetch("/api/messages", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-user-id": userId,
  },
  body: JSON.stringify({
    subject: "Тема сообщения",
    message: "Текст сообщения",
    priority: "medium",
  }),
})
```

### Получить уведомления
```javascript
const response = await fetch("/api/notifications?type=unread&limit=50", {
  headers: {
    "x-user-id": userId,
  },
})
```

### Создать уведомление (системное)
```javascript
const response = await fetch("/api/notifications", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    userId: "user-uuid",
    type: "success",
    title: "Депозит зачислен",
    message: "Ваш депозит на сумму $1000 успешно зачислен",
    icon: "DollarSign",
    color: "from-green-500 to-emerald-600",
  }),
})
```

### Отметить все уведомления как прочитанные
```javascript
const response = await fetch("/api/notifications", {
  method: "PATCH",
  headers: {
    "Content-Type": "application/json",
    "x-user-id": userId,
  },
  body: JSON.stringify({
    markAllAsRead: true,
  }),
})
```

## Интеграция с другими модулями

Вы можете автоматически создавать уведомления при различных событиях:

### При депозите
```javascript
await fetch("/api/notifications", {
  method: "POST",
  body: JSON.stringify({
    userId: userId,
    type: "success",
    title: "Депозит подтвержден",
    message: `Ваш депозит на сумму $${amount} успешно зачислен`,
    icon: "DollarSign",
    color: "from-green-500 to-emerald-600",
  }),
})
```

### При новом реферале
```javascript
await fetch("/api/notifications", {
  method: "POST",
  body: JSON.stringify({
    userId: referrerId,
    type: "referral",
    title: "Новый реферал",
    message: "По вашей ссылке зарегистрировался новый пользователь",
    icon: "Users",
    color: "from-purple-500 to-pink-600",
  }),
})
```

## Навигация

Обе страницы добавлены в боковое меню дашборда:
- 💬 **Сообщения** - с бейджем количества новых сообщений
- 🔔 **Уведомления** - с бейджем количества непрочитанных

## Безопасность

- ✅ Все API endpoints проверяют авторизацию через `x-user-id` header
- ✅ Пользователи видят только свои сообщения и уведомления
- ✅ SQL injection защита через параметризованные запросы
- ✅ Валидация входных данных

## Что дальше?

1. **Запустите bat-файл** для создания таблиц
2. **Перезапустите сервер** разработки
3. **Откройте** `/dashboard/messages` или `/dashboard/notifications`
4. **Протестируйте** отправку сообщений и просмотр уведомлений

Готово! Система сообщений и уведомлений полностью интегрирована с базой данных. 🎉
