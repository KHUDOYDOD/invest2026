# 🗄️ НАСТРОЙКА РЕАЛЬНОЙ БАЗЫ ДАННЫХ

## 📋 ЧТО ВЫ ПОЛУЧИТЕ

После настройки:
- ✅ Все данные сохраняются в базе данных
- ✅ Логины и пароли хранятся безопасно
- ✅ Регистрация создает реальных пользователей
- ✅ Вход работает с реальными данными
- ✅ Все транзакции сохраняются
- ✅ Никаких демо-данных

## 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Установите PostgreSQL

1. **Скачайте PostgreSQL:**
   - Откройте: https://www.postgresql.org/download/windows/
   - Скачайте последнюю версию (16.x)

2. **Запустите установщик:**
   - Запустите скачанный файл
   - Нажмите "Next" несколько раз
   - **ВАЖНО:** Запомните пароль для пользователя `postgres`!
   - Порт оставьте: `5432`
   - Locale оставьте по умолчанию
   - Дождитесь окончания установки

3. **Проверьте установку:**
   ```bash
   psql --version
   ```
   Должно показать версию PostgreSQL

### Шаг 2: Обновите .env.local

1. **Откройте файл `.env.local`** в корне проекта

2. **Замените пароль:**
   ```env
   DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/investpro
   ```
   
   Замените `ВАШ_ПАРОЛЬ` на пароль, который вы установили при установке PostgreSQL

3. **Сохраните файл**

### Шаг 3: Запустите автоматическую настройку

Просто запустите этот файл (двойной клик):

```
setup-database.bat
```

Скрипт автоматически:
- ✅ Создаст базу данных `investpro`
- ✅ Создаст все таблицы
- ✅ Настроит структуру
- ✅ Спросит, нужно ли очистить демо-данные

### Шаг 4: Создайте супер-администратора

Запустите (двойной клик):

```
create-super-admin.bat
```

Это создаст первого пользователя:
- Email: `creator@investpro.com`
- Пароль: `SuperAdmin2025!`
- Роль: Супер-администратор (полный контроль)

### Шаг 5: Перезапустите сервер

1. **Остановите текущий сервер:**
   - В терминале нажмите `Ctrl+C`

2. **Запустите снова:**
   ```bash
   npm run dev
   ```

3. **Откройте сайт:**
   http://localhost:3000

### Шаг 6: Войдите с реальными данными

1. Откройте: http://localhost:3000/login
2. Введите:
   - Email: `creator@investpro.com`
   - Пароль: `SuperAdmin2025!`
3. Нажмите "Войти"

**Готово!** Теперь вы работаете с реальной базой данных! 🎉

## 🔍 ПРОВЕРКА РАБОТЫ

### Проверьте подключение к БД:

```bash
psql -U postgres -d investpro -c "SELECT version();"
```

Введите пароль postgres, должна показаться версия PostgreSQL.

### Проверьте пользователей:

```bash
psql -U postgres -d investpro
```

Затем в psql:
```sql
SELECT email, full_name, role_id FROM users;
\q
```

Должен показаться ваш супер-админ!

## 📝 РЕГИСТРАЦИЯ НОВЫХ ПОЛЬЗОВАТЕЛЕЙ

Теперь регистрация работает по-настоящему:

1. Откройте: http://localhost:3000/register
2. Заполните форму
3. Нажмите "Зарегистрироваться"
4. Пользователь сохранится в базе данных!
5. Можете войти с этими данными

## 🔐 БЕЗОПАСНОСТЬ

### Пароли хранятся безопасно:
- ✅ Хешируются с помощью bcrypt
- ✅ Никогда не хранятся в открытом виде
- ✅ Невозможно восстановить оригинальный пароль

### Рекомендации:
1. **Измените пароль супер-админа** после первого входа
2. **Используйте сильные пароли** (минимум 8 символов)
3. **Не делитесь паролем** от базы данных
4. **Регулярно делайте бэкапы**

## 💾 БЭКАПЫ

### Создание бэкапа:

```bash
pg_dump -U postgres investpro > backup_$(date +%Y%m%d).sql
```

### Восстановление из бэкапа:

```bash
psql -U postgres -d investpro < backup_20250127.sql
```

## 🔧 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "ECONNREFUSED"

**Причина:** PostgreSQL не запущен

**Решение:**
```bash
# Проверьте службу
net start postgresql-x64-16
```

### Проблема: "password authentication failed"

**Причина:** Неверный пароль в `.env.local`

**Решение:**
1. Откройте `.env.local`
2. Проверьте пароль в `DATABASE_URL`
3. Убедитесь, что это пароль от PostgreSQL

### Проблема: "database does not exist"

**Причина:** База данных не создана

**Решение:**
```bash
psql -U postgres
CREATE DATABASE investpro;
\q
```

Затем запустите: `setup-database.bat`

### Проблема: "relation does not exist"

**Причина:** Таблицы не созданы

**Решение:**
```bash
psql -U postgres -d investpro -f complete-database-setup.sql
```

## 📊 УПРАВЛЕНИЕ БАЗОЙ ДАННЫХ

### Просмотр всех пользователей:

```sql
psql -U postgres -d investpro
SELECT id, email, full_name, role_id, balance FROM users;
```

### Изменение роли пользователя:

```sql
-- Сделать пользователя админом
UPDATE users SET role_id = 2 WHERE email = 'user@example.com';

-- Сделать пользователя супер-админом
UPDATE users SET role_id = 1 WHERE email = 'user@example.com';
```

### Просмотр транзакций:

```sql
SELECT * FROM transactions ORDER BY created_at DESC LIMIT 10;
```

### Просмотр инвестиций:

```sql
SELECT * FROM investments ORDER BY created_at DESC LIMIT 10;
```

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После настройки базы данных:

1. ✅ Войдите как супер-админ
2. ✅ Настройте инвестиционные планы
3. ✅ Настройте платежные системы
4. ✅ Добавьте контент (новости, отзывы)
5. ✅ Создайте других администраторов
6. ✅ Начните привлекать пользователей!

## 📞 НУЖНА ПОМОЩЬ?

### Если что-то не работает:

1. **Проверьте логи сервера** в терминале
2. **Проверьте консоль браузера** (F12)
3. **Проверьте, что PostgreSQL запущен**
4. **Проверьте пароль в `.env.local`**

### Полезные команды:

```bash
# Проверка подключения
psql -U postgres -d investpro -c "SELECT 1;"

# Просмотр таблиц
psql -U postgres -d investpro -c "\dt"

# Размер базы данных
psql -U postgres -d investpro -c "SELECT pg_size_pretty(pg_database_size('investpro'));"
```

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `investpro` создана
- [ ] Таблицы созданы (запущен `complete-database-setup.sql`)
- [ ] Файл `.env.local` настроен с правильным паролем
- [ ] Супер-администратор создан
- [ ] Сервер перезапущен
- [ ] Можете войти с реальными данными
- [ ] Регистрация работает и сохраняет пользователей

## 🎉 ГОТОВО!

Теперь у вас:
- ✅ Реальная база данных PostgreSQL
- ✅ Все данные сохраняются
- ✅ Логины и пароли в безопасности
- ✅ Регистрация создает реальных пользователей
- ✅ Никаких демо-данных

**Ваша инвестиционная платформа готова к работе!** 🚀

---

**Важно:** Демо-режим все еще работает как запасной вариант. Если база данных станет недоступна, система автоматически переключится на демо-режим.
