# 🌐 Настройка домена для локального сайта

## 📋 Есть 3 способа:

---

## 🏠 Способ 1: Локальный домен (только на вашем компьютере)

### Что это:
- Сайт работает только на вашем компьютере
- Доступен по красивому имени вместо localhost
- Не нужен интернет
- **БЕСПЛАТНО**

### Как настроить:

**Автоматически:**
```bash
настроить-локальный-домен.bat
```
(Запустите от имени администратора!)

**Вручную:**
1. Откройте Блокнот от имени администратора
2. Откройте файл: `C:\Windows\System32\drivers\etc\hosts`
3. Добавьте строку:
   ```
   127.0.0.1 invest.local
   ```
4. Сохраните файл

### Использование:
```bash
npm run dev
```
Откройте: `http://invest.local:3000`

### Убрать :3000 из адреса:
```bash
npm run dev -- -p 80
```
Откройте: `http://invest.local`

---

## 🌍 Способ 2: Публичный туннель (доступен из интернета)

### Что это:
- Ваш локальный сайт доступен из интернета
- Получаете настоящий URL
- Можете показать сайт кому угодно
- **БЕСПЛАТНО**

### Варианты:

#### A) **ngrok** (самый популярный)

**Установка:**
1. Скачайте: https://ngrok.com/download
2. Распакуйте в папку проекта
3. Зарегистрируйтесь: https://dashboard.ngrok.com/signup

**Использование:**
```bash
# Запустите сервер
npm run dev

# В другом терминале:
ngrok http 3000
```

Получите URL типа: `https://abc123.ngrok.io`

**Свой домен (платно $8/месяц):**
```bash
ngrok http 3000 --domain=yoursite.com
```

#### B) **Cloudflare Tunnel** (бесплатно + свой домен!)

**Установка:**
```bash
npm install -g cloudflared
```

**Использование:**
```bash
# Запустите сервер
npm run dev

# В другом терминале:
cloudflared tunnel --url http://localhost:3000
```

**Свой домен (БЕСПЛАТНО!):**
1. Зарегистрируйтесь: https://dash.cloudflare.com
2. Добавьте свой домен
3. Создайте туннель:
```bash
cloudflared tunnel create invest
cloudflared tunnel route dns invest yoursite.com
cloudflared tunnel run invest
```

#### C) **localtunnel** (самый простой)

**Установка:**
```bash
npm install -g localtunnel
```

**Использование:**
```bash
# Запустите сервер
npm run dev

# В другом терминале:
lt --port 3000
```

Получите URL типа: `https://random-name.loca.lt`

**Свой поддомен:**
```bash
lt --port 3000 --subdomain myinvest
```
URL: `https://myinvest.loca.lt`

---

## 🏢 Способ 3: Настоящий хостинг + домен

### Что это:
- Сайт работает 24/7
- Настоящий домен (yoursite.com)
- Доступен всем из интернета
- Профессиональное решение

### Варианты:

#### **Vercel** (рекомендую)
- ✅ Бесплатный хостинг
- ✅ Свой домен бесплатно
- ✅ Автоматический SSL
- ✅ Настройка за 5 минут

#### **Netlify**
- ✅ Бесплатный хостинг
- ✅ Свой домен бесплатно
- ✅ Простая настройка

#### **Cloudflare Pages**
- ✅ Бесплатный хостинг
- ✅ Свой домен бесплатно
- ⚠️ Проблемы с Next.js API

---

## 🎯 Какой способ выбрать?

### Для разработки и тестов:
- **Способ 1** (локальный домен) - если нужно только вам
- **Способ 2** (туннель) - если нужно показать другим

### Для продакшена:
- **Способ 3** (хостинг) - для настоящего сайта

---

## 📝 Примеры использования:

### Локальный домен:
```bash
# 1. Настройте домен
настроить-локальный-домен.bat

# 2. Запустите сервер
npm run dev

# 3. Откройте
http://invest.local:3000
```

### ngrok туннель:
```bash
# Терминал 1:
npm run dev

# Терминал 2:
ngrok http 3000

# Получите URL и поделитесь!
```

### Cloudflare Tunnel:
```bash
# Терминал 1:
npm run dev

# Терминал 2:
cloudflared tunnel --url http://localhost:3000

# Получите публичный URL!
```

---

## 🆘 Нужна помощь?

Скажите, какой способ хотите использовать, и я помогу настроить!

1. **Локальный домен** - только для вас
2. **Туннель** - показать другим
3. **Хостинг** - настоящий сайт 24/7
