# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С ЛОГИНОМ

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО

Я исправил проблему с входом в систему:

### 1. Обновлен API логина (`app/api/login/route.ts`):
- ✅ Исправлено чтение поля `password_hash` вместо `password`
- ✅ Добавлена поддержка входа по email, login или full_name
- ✅ Добавлена проверка активности аккаунта
- ✅ Исправлено получение роли из таблицы `user_roles`
- ✅ Добавлено обновление времени последнего входа
- ✅ Добавлен автоматический redirect для админов

### 2. Обновлен скрипт создания супер-админа:
- ✅ Исправлена работа с UUID вместо SERIAL
- ✅ Добавлено создание роли `super_admin`
- ✅ Исправлено использование `password_hash`
- ✅ Добавлено поле `login`

## 🚀 КАК ИСПОЛЬЗОВАТЬ

### Вариант 1: Вход с демо-данными

Если база данных не настроена, используйте демо-данные:

```
Email: admin@demo.com
Пароль: admin123
```

### Вариант 2: Создание супер-админа

Если база данных настроена:

```bash
# 1. Создайте супер-админа
create-super-admin.bat

# 2. Войдите с данными:
Email: creator@investpro.com
Пароль: SuperAdmin2025!
```

## 🔍 ПРОВЕРКА РАБОТЫ

1. Откройте: http://localhost:3000/login
2. Введите email и пароль
3. Нажмите "Войти"
4. Вы должны быть перенаправлены в личный кабинет

## 🐛 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ

### Проблема: "Неверные учетные данные"

**Причина:** Пользователь не существует в базе данных

**Решение:**
1. Проверьте, что база данных настроена:
   ```bash
   psql -U postgres -d investpro -c "SELECT email FROM users;"
   ```

2. Если база пустая, создайте супер-админа:
   ```bash
   create-super-admin.bat
   ```

3. Или используйте демо-данные (без базы данных)

### Проблема: "Ошибка сервера"

**Причина:** База данных не подключена

**Решение:**
1. Проверьте `.env.local`:
   ```env
   DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/investpro
   ```

2. Убедитесь, что PostgreSQL запущен:
   ```bash
   # Windows
   net start postgresql-x64-16
   ```

3. Проверьте подключение:
   ```bash
   psql -U postgres -d investpro -c "SELECT version();"
   ```

### Проблема: "Аккаунт деактивирован"

**Причина:** Поле `is_active` = false

**Решение:**
```sql
psql -U postgres -d investpro
UPDATE users SET is_active = true WHERE email = 'ваш_email@example.com';
```

## 📊 СТРУКТУРА БАЗЫ ДАННЫХ

Таблица `users` использует:
- `id` - UUID (не SERIAL)
- `password_hash` - хешированный пароль (не `password`)
- `role_id` - ссылка на таблицу `user_roles`
- `is_active` - статус активности
- `login` - логин для входа (опционально)

## 🔐 РОЛИ В СИСТЕМЕ

Роли хранятся в таблице `user_roles`:
- `super_admin` - полный контроль (создатель)
- `admin` - администратор
- `user` - обычный пользователь

## ✅ ТЕКУЩЕЕ СОСТОЯНИЕ

После исправлений:
- ✅ Вход работает с email, login или full_name
- ✅ Пароли правильно проверяются
- ✅ Роли корректно определяются
- ✅ Админы перенаправляются в админ-панель
- ✅ Обычные пользователи - в личный кабинет

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. Перезагрузите страницу: http://localhost:3000/login
2. Попробуйте войти
3. Если не работает - проверьте консоль браузера (F12)
4. Проверьте логи сервера в терминале

---

**Проблема решена! Теперь вход должен работать корректно! ✅**
