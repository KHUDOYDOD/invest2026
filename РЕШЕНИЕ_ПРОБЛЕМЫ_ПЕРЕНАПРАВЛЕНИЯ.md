# 🔧 РЕШЕНИЕ ПРОБЛЕМЫ ПЕРЕНАПРАВЛЕНИЯ С IP НА ДОМЕН

## 🎯 ПРОБЛЕМА:
При заходе на http://213.171.31.215 происходит перенаправление на tradepo.ru, но домен не работает.

## ✅ СТАТУС СЕРВЕРА:
- **Сервер работает корректно** на порту 80
- **Nginx отключен** - нет серверных перенаправлений  
- **Next.js приложение** работает напрямую на порту 80
- **Тесты с сервера** показывают HTTP 200 OK без перенаправлений

## 🔍 ПРИЧИНА ПРОБЛЕМЫ:
Перенаправление происходит **НЕ на сервере**, а в браузере из-за:
1. **HSTS кэша** (HTTP Strict Transport Security)
2. **DNS кэша** 
3. **Браузерного кэша** старых перенаправлений
4. **Истории браузера** с предыдущими HTTPS настройками

## 🛠️ РЕШЕНИЯ (попробуйте по порядку):

### 1️⃣ ОЧИСТКА КЭША БРАУЗЕРА
```
Ctrl + F5 (принудительное обновление)
или
Ctrl + Shift + R (жесткое обновление)
```

### 2️⃣ РЕЖИМ ИНКОГНИТО
- Откройте **новое окно инкогнито** (Ctrl+Shift+N)
- Перейдите на http://213.171.31.215
- В инкогнито нет кэша и истории

### 3️⃣ ОЧИСТКА HSTS КЭША (Chrome)
1. Откройте: `chrome://net-internals/#hsts`
2. В разделе "Delete domain security policies"
3. Введите: `213.171.31.215`
4. Нажмите "Delete"
5. Также удалите: `tradepo.ru`

### 4️⃣ ОЧИСТКА DNS КЭША
**Windows:**
```cmd
ipconfig /flushdns
```

**macOS:**
```bash
sudo dscacheutil -flushcache
```

**Linux:**
```bash
sudo systemctl restart systemd-resolved
```

### 5️⃣ ДРУГОЙ БРАУЗЕР
Попробуйте открыть в **другом браузере** (Firefox, Edge, Safari)

### 6️⃣ ПРЯМАЯ ССЫЛКА
Скопируйте и вставьте в адресную строку:
```
http://213.171.31.215/
```

## 🧪 ТЕСТОВЫЕ ССЫЛКИ:

### Основные страницы:
- **Главная**: http://213.171.31.215/
- **API статуса**: http://213.171.31.215/api/site-status  
- **Админ панель**: http://213.171.31.215/admin/project-launches

### Для входа в админку:
- **Логин**: admin@example.com
- **Пароль**: X11021997x

## 🔧 ТЕХНИЧЕСКОЕ ПОДТВЕРЖДЕНИЕ:

Проверка с сервера показывает, что все работает:
```bash
# Тест доступности
curl -I http://213.171.31.215
# Результат: HTTP/1.1 200 OK ✅

# Тест API
curl http://213.171.31.215/api/site-status  
# Результат: JSON с данными ✅

# Нет перенаправлений
curl -v http://213.171.31.215 | grep Location
# Результат: пусто (нет Location заголовков) ✅
```

## 💡 ПОЧЕМУ ЭТО ПРОИСХОДИТ:

Ранее на сервере был настроен **автоматический HTTPS редирект**. Браузеры запомнили это и теперь **автоматически** пытаются перенаправить на HTTPS версию домена, даже когда вы вводите IP адрес.

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После выполнения любого из решений вы должны увидеть:
- **Главную страницу** InvestPro
- **Заголовок**: "InvestPro - Профессиональная инвестиционная платформа"  
- **Яркий дизайн** с градиентами
- **Рабочие ссылки** на регистрацию и вход

## 🚨 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ:

1. **Попробуйте с другого устройства** (телефон, планшет)
2. **Попробуйте с другой сети** (мобильный интернет)
3. **Подождите 24 часа** для обновления DNS кэша

**Сервер работает корректно - проблема только в кэшировании на стороне клиента!** ✅