# 📘 Руководство по настройке базы данных в Supabase

## 🎯 Цель
Создать все недостающие таблицы в Supabase для корректной работы приложения на Vercel.

## 📋 Шаги настройки

### 1. Откройте Supabase Dashboard
1. Перейдите на https://supabase.com
2. Войдите в свой аккаунт
3. Выберите проект `kdfxytlaxrcrtsxvqilg`

### 2. Откройте SQL Editor
1. В левом меню нажмите на **SQL Editor**
2. Нажмите **New Query**

### 3. Выполните SQL скрипт
1. Откройте файл `supabase-setup.sql` из корня проекта
2. Скопируйте весь содержимое файла
3. Вставьте в SQL Editor в Supabase
4. Нажмите **Run** (или Ctrl+Enter)

### 4. Проверьте результат
После выполнения скрипта вы должны увидеть сообщение об успешном выполнении. В конце скрипта есть запрос, который покажет все созданные таблицы:

```sql
SELECT 
    table_name,
    (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = t.table_name) as column_count
FROM information_schema.tables t
WHERE table_schema = 'public' 
    AND table_type = 'BASE TABLE'
    AND table_name IN (
        'platform_statistics',
        'site_settings',
        'appearance_settings',
        'notification_settings',
        'testimonials',
        'messages',
        'user_notifications',
        'user_notification_preferences'
    )
ORDER BY table_name;
```

## ✅ Созданные таблицы

### 1. `platform_statistics`
Статистика платформы для главной страницы:
- Количество пользователей
- Сумма инвестиций
- Выплаченная прибыль
- Средняя доходность

### 2. `site_settings`
Общие настройки сайта:
- Название сайта
- Контактный email
- Лимиты депозитов/выводов
- Комиссии и бонусы

### 3. `appearance_settings`
Настройки внешнего вида:
- Цветовая схема
- Логотип и favicon
- Темная/светлая тема

### 4. `notification_settings`
Глобальные настройки уведомлений:
- Email уведомления
- SMS уведомления
- Push уведомления

### 5. `testimonials`
Отзывы пользователей:
- Рейтинг (1-5 звезд)
- Заголовок и текст отзыва
- Статус модерации

### 6. `messages`
Сообщения пользователей в поддержку:
- Тема и текст сообщения
- Ответ администратора
- Статус (прочитано/не прочитано)

### 7. `user_notifications`
Уведомления для пользователей:
- Заголовок и текст
- Тип (info/success/warning/error)
- Статус прочтения

### 8. `user_notification_preferences`
Персональные настройки уведомлений пользователей:
- Включение/выключение разных типов уведомлений

## 🔄 После выполнения скрипта

### 1. Проверьте API эндпоинты
Откройте в браузере:
- https://invest2025-main.vercel.app/api/statistics
- https://invest2025-main.vercel.app/api/settings/site
- https://invest2025-main.vercel.app/api/testimonials

Все эндпоинты должны возвращать данные без ошибок.

### 2. Проверьте главную страницу
Откройте https://invest2025-main.vercel.app и убедитесь, что:
- Статистика отображается корректно
- Нет ошибок в консоли браузера
- Все компоненты загружаются

## 🛠️ Дополнительные настройки (опционально)

### Добавление тестовых отзывов
Если вы хотите добавить тестовые отзывы, выполните:

```sql
-- Сначала убедитесь, что у вас есть тестовые пользователи
INSERT INTO testimonials (user_id, rating, title, content, status, approved_at) 
SELECT 
    id,
    5,
    'Отличная платформа!',
    'Я очень доволен работой платформы. Быстрые выплаты и отличная поддержка.',
    'approved',
    NOW()
FROM users 
WHERE email = 'test@example.com'
LIMIT 1;
```

### Обновление статистики
Чтобы обновить статистику на главной странице:

```sql
UPDATE platform_statistics 
SET 
    users_count = 20000,
    investments_amount = 5000000,
    payouts_amount = 3000000,
    updated_at = NOW()
WHERE id = 1;
```

## ❗ Важные замечания

1. **Не удаляйте существующие таблицы** - скрипт использует `CREATE TABLE IF NOT EXISTS`, поэтому безопасен для повторного выполнения

2. **Резервное копирование** - перед выполнением скрипта рекомендуется создать backup базы данных в Supabase

3. **Права доступа** - убедитесь, что у вас есть права на создание таблиц в Supabase

4. **Проверка связей** - скрипт создает внешние ключи к таблице `users`, убедитесь, что она существует

## 🐛 Решение проблем

### Ошибка: "relation users does not exist"
**Решение**: Сначала создайте таблицу `users` из файла `complete-database-setup.sql`

### Ошибка: "permission denied"
**Решение**: Проверьте права доступа в Supabase Dashboard → Settings → Database

### Ошибка: "duplicate key value"
**Решение**: Это нормально, если таблицы уже существуют. Скрипт использует `ON CONFLICT DO NOTHING`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Supabase Dashboard → Logs
2. Проверьте логи деплоя в Vercel
3. Проверьте переменные окружения в Vercel

## ✅ Готово!

После выполнения всех шагов ваше приложение должно работать полностью корректно на https://invest2025-main.vercel.app
