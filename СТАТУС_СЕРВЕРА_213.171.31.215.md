# 🎯 СТАТУС СЕРВЕРА 213.171.31.215

## ✅ РЕЗУЛЬТАТ ПРОВЕРКИ
- **SSH порт 22:** ✅ ДОСТУПЕН
- **Ping:** ❌ Заблокирован (это нормально)
- **Статус:** 🟢 ГОТОВ К РАБОТЕ

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС

### Вариант 1: Веб-консоль (РЕКОМЕНДУЕТСЯ)
1. Откройте панель управления VPS
2. Найдите веб-консоль для сервера 213.171.31.215
3. Войдите как `root11` с паролем `$X11021997x$`
4. Скопируйте команды из файла `КОМАНДЫ_НОВЫЙ_СЕРВЕР.txt`
5. Выполните их по порядку

### Вариант 2: SSH клиент
Если у вас есть SSH клиент с поддержкой паролей:
```bash
ssh root11@213.171.31.215
# Пароль: $X11021997x$
```

## 📋 КОМАНДЫ ДЛЯ БЫСТРОЙ УСТАНОВКИ

Скопируйте и выполните в веб-консоли:

```bash
# 1. Обновление системы
apt update && apt upgrade -y

# 2. Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
apt-get install -y nodejs

# 3. Установка зависимостей
npm install -g pm2
apt-get install -y nginx git

# 4. Клонирование проекта
cd /root
git clone https://github.com/KHUDOYDOD/invest2026.git
cd invest2026

# 5. Установка зависимостей проекта
npm install

# 6. Создание конфигурации
cat > .env.production << 'EOF'
DATABASE_URL=postgresql://neondb_owner:npg_w5yC0HdchuEB@ep-bold-grass-abge4ccn-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require
NEXTAUTH_SECRET=your-secret-key-here-change-this-in-production-2026
NEXTAUTH_URL=http://213.171.31.215
JWT_SECRET=your-jwt-secret-key-here-change-this-in-production-2026
NODE_ENV=production
EOF

# 7. Сборка проекта
npm run build

# 8. Настройка Nginx
cat > /etc/nginx/sites-available/default << 'EOF'
server {
    listen 80;
    server_name 213.171.31.215;
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
EOF

# 9. Запуск сервисов
systemctl restart nginx
systemctl enable nginx

# 10. Запуск приложения
pm2 start npm --name investpro -- start
pm2 startup
pm2 save
```

## 🌐 ПОСЛЕ УСТАНОВКИ

Сайт будет доступен по адресам:
- **Главная:** http://213.171.31.215
- **Админка:** http://213.171.31.215/admin/dashboard

**Данные для входа в админку:**
- Логин: `admin`
- Пароль: `X11021997x`

## 🔧 ПРОВЕРКА РАБОТЫ

После установки выполните:
```bash
pm2 status
systemctl status nginx
curl http://localhost:3000
```

## 📞 ЕСЛИ НУЖНА ПОМОЩЬ

1. Проверьте логи: `pm2 logs investpro`
2. Перезапустите: `pm2 restart investpro`
3. Проверьте Nginx: `systemctl status nginx`