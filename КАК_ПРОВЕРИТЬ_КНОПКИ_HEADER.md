# Как проверить работу кнопок в Header личного кабинета

## Что было сделано

### 1. Кнопка поиска 🔍
**Функционал:**
- Открывает поле поиска
- Ищет по разделам дашборда в реальном времени
- Показывает результаты с иконками
- Переход по клику на результат

**Как проверить:**
1. Зайдите в личный кабинет (http://localhost:3000/dashboard)
2. Нажмите на иконку 🔍 в правом верхнем углу
3. Введите текст, например:
   - "пополн" → найдёт "Пополнение"
   - "транз" → найдёт "Транзакции"
   - "настр" → найдёт "Настройки"
4. Кликните на результат → должна открыться нужная страница

### 2. Кнопка темы ☀️/🌙
**Функционал:**
- Переключает между светлой и тёмной темой
- Сохраняет выбор в localStorage
- Применяет тему ко всему сайту

**Как проверить:**
1. Зайдите в личный кабинет
2. Нажмите на иконку ☀️ (или 🌙)
3. Тема должна измениться
4. Перезагрузите страницу → тема должна сохраниться

### 3. Переключатель языков 🌐
**Функционал:**
- Показывает флаг и название текущего языка
- Открывает меню с 6 языками
- Переводит все тексты в header

**Как проверить:**
1. Найдите кнопку с флагом и названием языка
2. Нажмите на неё
3. Выберите другой язык
4. Все тексты в header должны измениться

## Если кнопки не работают

### Проблема 1: Кнопки не кликаются
**Решение:**
```bash
# Перезапустите сервер
npm run dev
```

### Проблема 2: Поиск не показывает результаты
**Причина:** Переводы не загружены
**Решение:** Проверьте что файлы `lib/i18n/locales/*.json` существуют

### Проблема 3: Тема не меняется
**Причина:** ThemeProvider не работает
**Решение:** Проверьте `app/layout.tsx` - должен быть `<ThemeProvider>`

### Проблема 4: Язык не переключается
**Причина:** LanguageProvider не работает
**Решение:** Проверьте `app/layout.tsx` - должен быть `<LanguageProvider>`

## Проверка в консоли браузера

Откройте консоль (F12) и проверьте:

```javascript
// Проверка темы
localStorage.getItem('theme')

// Проверка языка
localStorage.getItem('language')

// Проверка ошибок
// Не должно быть красных ошибок в консоли
```

## Файлы которые были изменены

1. `components/dashboard/header.tsx` - добавлен функционал поиска и темы
2. `components/language-switcher.tsx` - улучшен дизайн с флагами
3. `lib/i18n/locales/*.json` - добавлены переводы для header

## Что должно работать

✅ Поиск по разделам дашборда
✅ Переключение темы (светлая/тёмная)
✅ Переключение языка (6 языков)
✅ Сохранение настроек в localStorage
✅ Анимации при переключении

## Скриншот как должно выглядеть

```
┌─────────────────────────────────────────────────────────────┐
│ [IP] InvestPro    🕐 12:34  🟢 Онлайн  🏠 Личный кабинет   │
│                                                             │
│         🔍  ☀️  [🇷🇺 Русский 🌐]  💬²  🔔²  👤 Иван ▼     │
│         ↑   ↑         ↑                                     │
│      Поиск Тема    Язык                                     │
└─────────────────────────────────────────────────────────────┘
```

## Если всё равно не работает

1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Перезапустите сервер разработки
3. Проверьте что нет ошибок в консоли браузера (F12)
4. Убедитесь что вы на странице `/dashboard` (не на главной)
