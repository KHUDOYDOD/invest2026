# НАСТРОЙКА 4VPS ДЛЯ NEXT.JS

## Шаг 1: Регистрация и заказ VPS

1. Перейдите на https://4vps.su/
2. Зарегистрируйтесь
3. Выберите тариф (минимум 1GB RAM, 1 CPU)
4. Выберите ОС: **Ubuntu 22.04 LTS**
5. Оплатите (от 150₽/мес)

## Шаг 2: Получение доступа

После создания VPS вы получите:
- IP адрес сервера
- Логин: `root`
- Пароль (в письме или личном кабинете)

## Шаг 3: Подключение к серверу

### Windows (PowerShell):
```powershell
ssh root@ВАШ_IP_АДРЕС
# Введите пароль когда попросит
```

### Первый вход - смена пароля:
```bash
passwd
# Введите новый надежный пароль дважды
```

## Шаг 4: Установка необходимого ПО

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Установка PM2 (менеджер процессов)
npm install -g pm2

# Установка Nginx (веб-сервер)
apt install -y nginx

# Установка Git
apt install -y git

# Проверка установки
node --version
npm --version
```

## Шаг 5: Клонирование проекта

```bash
# Переход в домашнюю директорию
cd /root

# Клонирование вашего проекта
git clone https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git
cd ВАШ_РЕПОЗИТОРИЙ

# Установка зависимостей
npm install
```

## Шаг 6: Настройка переменных окружения

```bash
# Создание .env файла
nano .env.production
```

Вставьте ваши переменные:
```env
DATABASE_URL=postgresql://user:password@host:5432/database
NEXTAUTH_SECRET=ваш_секретный_ключ_минимум_32_символа
NEXTAUTH_URL=http://ВАШ_IP_АДРЕС:3000
NODE_ENV=production
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

## Шаг 7: Сборка проекта

```bash
# Сборка Next.js приложения
npm run build
```

## Шаг 8: Запуск с PM2

```bash
# Запуск приложения
pm2 start npm --name "investpro" -- start

# Автозапуск при перезагрузке сервера
pm2 startup
pm2 save

# Проверка статуса
pm2 status
pm2 logs investpro
```

## Шаг 9: Настройка Nginx (опционально)

Если хотите использовать порт 80 вместо 3000:

```bash
nano /etc/nginx/sites-available/investpro
```

Вставьте конфигурацию:
```nginx
server {
    listen 80;
    server_name ВАШ_IP_АДРЕС;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируйте конфигурацию:
```bash
ln -s /etc/nginx/sites-available/investpro /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

Обновите NEXTAUTH_URL:
```bash
nano .env.production
# Измените на: NEXTAUTH_URL=http://ВАШ_IP_АДРЕС
```

Перезапустите приложение:
```bash
pm2 restart investpro
```

## Шаг 10: Настройка файрвола

```bash
# Установка UFW
apt install -y ufw

# Разрешение SSH, HTTP, HTTPS
ufw allow 22
ufw allow 80
ufw allow 443
ufw allow 3000

# Включение файрвола
ufw enable
ufw status
```

## Проверка работы

Откройте в браузере:
- С Nginx: `http://ВАШ_IP_АДРЕС`
- Без Nginx: `http://ВАШ_IP_АДРЕС:3000`

## Полезные команды PM2

```bash
# Просмотр логов
pm2 logs investpro

# Перезапуск
pm2 restart investpro

# Остановка
pm2 stop investpro

# Удаление из PM2
pm2 delete investpro

# Мониторинг
pm2 monit
```

## Обновление проекта

```bash
cd /root/ВАШ_РЕПОЗИТОРИЙ
git pull
npm install
npm run build
pm2 restart investpro
```

## Подключение домена (опционально)

1. В панели 4VPS или у регистратора домена добавьте A-запись:
   ```
   @ -> ВАШ_IP_АДРЕС
   www -> ВАШ_IP_АДРЕС
   ```

2. Обновите конфигурацию Nginx:
   ```bash
   nano /etc/nginx/sites-available/investpro
   # Измените server_name на: server_name ваш-домен.ru www.ваш-домен.ru;
   ```

3. Установите SSL сертификат:
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
   ```

4. Обновите NEXTAUTH_URL:
   ```bash
   nano .env.production
   # Измените на: NEXTAUTH_URL=https://ваш-домен.ru
   pm2 restart investpro
   ```

## Решение проблем

### Приложение не запускается
```bash
pm2 logs investpro --lines 100
```

### Проверка портов
```bash
netstat -tulpn | grep :3000
```

### Перезапуск всех сервисов
```bash
pm2 restart all
systemctl restart nginx
```

### Проверка использования ресурсов
```bash
htop  # или
top
```

## Готово! 🎉

Ваше приложение работает на 4VPS.su!

**Следующие шаги:**
- Настройте регулярные бэкапы
- Настройте мониторинг
- Подключите домен и SSL
- Настройте автоматическое обновление из Git
