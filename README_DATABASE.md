# 🗄️ Настройка базы данных - Краткое руководство

## ⚡ Быстрый старт (3 минуты)

### Шаг 1: Запустите автоматическую настройку

```bash
setup-database.bat
```

Введите пароль PostgreSQL и следуйте инструкциям.

### Шаг 2: Запустите приложение

```bash
npm run dev
```

### Шаг 3: Создайте администратора

1. Откройте http://localhost:3000
2. Зарегистрируйтесь
3. Выполните в psql:
   ```sql
   UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
   ```

## 📁 Созданные файлы

### Конфигурация
- ✅ `.env.local` - Переменные окружения (обновите пароль!)

### Скрипты
- ✅ `scripts/clean-demo-data.js` - Очистка демо-данных (Node.js)
- ✅ `scripts/clean-database.sql` - Очистка демо-данных (SQL)
- ✅ `setup-database.bat` - Автоматическая настройка (Windows)

### Документация
- ✅ `DATABASE_SETUP.md` - Полная инструкция по настройке
- ✅ `QUICK_START.md` - Быстрый старт
- ✅ `README_DATABASE.md` - Это руководство

## 🔧 Что нужно сделать

### 1. Обновите .env.local

Откройте файл `.env.local` и замените:

```env
# Замените ВАШ_ПАРОЛЬ на реальный пароль PostgreSQL
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/investpro

# Замените на случайные строки для безопасности
JWT_SECRET=ваш_случайный_секретный_ключ_минимум_32_символа
NEXTAUTH_SECRET=другой_случайный_секретный_ключ_минимум_32_символа
```

**Генерация секретных ключей:**
```bash
# В PowerShell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

### 2. Установите PostgreSQL (если еще не установлен)

**Windows:**
- Скачайте: https://www.postgresql.org/download/windows/
- Установите с настройками по умолчанию
- Запомните пароль для пользователя `postgres`

**Проверка установки:**
```bash
psql --version
```

### 3. Очистите демо-данные

**Автоматически (рекомендуется):**
```bash
node scripts/clean-demo-data.js
```

**Вручную через SQL:**
```bash
psql -U postgres -d investpro -f scripts/clean-database.sql
```

## 📊 Что будет удалено

При очистке демо-данных удаляются:
- ❌ Все транзакции
- ❌ Все инвестиции
- ❌ Все запросы на депозит/вывод
- ❌ Все рефералы
- ❌ Все уведомления
- ❌ Все сообщения поддержки
- ❌ Вся активность пользователей
- ❌ Все отзывы
- ❌ Все новости
- ❌ Демо-пользователи (кроме админов)

**Сохраняется:**
- ✅ Структура базы данных
- ✅ Таблицы и индексы
- ✅ Администраторы (если есть)

## 🔍 Проверка подключения

```bash
# Проверка подключения к базе данных
psql -U postgres -d investpro -c "SELECT version();"

# Просмотр таблиц
psql -U postgres -d investpro -c "\dt"

# Просмотр пользователей
psql -U postgres -d investpro -c "SELECT id, email, role FROM users;"
```

## 🚨 Устранение проблем

### Ошибка: "password authentication failed"
```bash
# Сбросьте пароль
psql -U postgres
ALTER USER postgres PASSWORD 'новый_пароль';
\q

# Обновите .env.local с новым паролем
```

### Ошибка: "database does not exist"
```bash
psql -U postgres
CREATE DATABASE investpro;
\q
```

### Ошибка: "psql: command not found"
- Добавьте PostgreSQL в PATH
- Windows: `C:\Program Files\PostgreSQL\16\bin`

## 🔒 Безопасность

Перед продакшеном:

1. ✅ Измените все пароли
2. ✅ Сгенерируйте новые секретные ключи
3. ✅ Включите SSL для базы данных
4. ✅ Ограничьте доступ по IP
5. ✅ Настройте регулярные бэкапы

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте `DATABASE_SETUP.md` для подробной информации
2. Убедитесь, что PostgreSQL запущен
3. Проверьте логи: `psql -U postgres -d investpro`

## ✅ Чеклист готовности

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `investpro` создана
- [ ] Структура инициализирована
- [ ] `.env.local` настроен с правильным паролем
- [ ] Демо-данные очищены
- [ ] Создан администратор
- [ ] Приложение запускается без ошибок

## 🎉 Готово!

Теперь ваша база данных настроена и готова к работе!

**Следующие шаги:**
1. Запустите: `npm run dev`
2. Откройте: http://localhost:3000
3. Войдите как администратор
4. Начните настройку системы

---

**Полезные ссылки:**
- 📖 Полная документация: `DATABASE_SETUP.md`
- 🚀 Быстрый старт: `QUICK_START.md`
- 🔐 Демо-данные: `DEMO_CREDENTIALS.md`
