# 🌐 Привязка домена к Next.js на VPS

## ✅ Что готово:

- ✅ Next.js проект в GitHub: https://github.com/KHUDOYDOD/invest2026
- ✅ База данных Neon настроена
- ✅ Автоматические скрипты установки
- ✅ Конфигурация Nginx
- ✅ Настройка SSL

---

## 🚀 Быстрая установка (5 минут)

### На VPS выполните:

```bash
# 1. Подключитесь к VPS
ssh root@ВАШ_IP_VPS

# 2. Скачайте и запустите скрипт
wget https://raw.githubusercontent.com/KHUDOYDOD/invest2026/main/vps-setup.sh
bash vps-setup.sh
```

Скрипт спросит:
- Ваш домен (например: `mysite.com`)
- IP вашего VPS

Затем автоматически:
- Установит Node.js, PM2, Nginx, Certbot
- Клонирует проект из GitHub
- Настроит окружение (.env)
- Соберет проект
- Запустит с PM2
- Настроит Nginx
- Настроит Firewall

### В панели управления доменом:

Добавьте A-записи:

```
Тип: A
Имя: @
Значение: IP_ВАШЕГО_VPS
TTL: 3600
```

```
Тип: A
Имя: www
Значение: IP_ВАШЕГО_VPS
TTL: 3600
```

### Подождите 15-30 минут (DNS обновление)

### Установите SSL:

```bash
certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

## ✅ Готово!

Ваш сайт доступен: `https://ваш-домен.com`

---

## 🔄 Обновление сайта

Когда нужно обновить код:

```bash
ssh root@ВАШ_IP_VPS
bash /var/www/invest2026/vps-update.sh
```

Или вручную:
```bash
cd /var/www/invest2026
git pull origin main
npm install
npm run build
pm2 restart invest2026
```

---

## 🔧 Управление

### Просмотр логов:
```bash
pm2 logs invest2026
```

### Перезапуск:
```bash
pm2 restart invest2026
```

### Статус:
```bash
pm2 status
```

### Перезапуск Nginx:
```bash
systemctl restart nginx
```

### Просмотр логов Nginx:
```bash
tail -f /var/log/nginx/error.log
tail -f /var/log/nginx/access.log
```

---

## 📁 Структура на VPS

```
/var/www/invest2026/          # Папка проекта
├── .env.production           # Переменные окружения
├── .next/                    # Собранный проект
├── node_modules/             # Зависимости
└── ...

/etc/nginx/sites-available/invest2026  # Конфигурация Nginx
/etc/nginx/sites-enabled/invest2026    # Активная конфигурация
```

---

## 🔐 Безопасность

Скрипт автоматически настраивает:
- ✅ UFW Firewall (порты 22, 80, 443)
- ✅ SSL сертификат (HTTPS)
- ✅ Автообновление SSL (каждые 90 дней)

---

## 🆘 Решение проблем

### Сайт не открывается:

1. **Проверьте DNS:**
   ```bash
   nslookup ваш-домен.com
   ```
   Должен показать IP вашего VPS

2. **Проверьте статус сервисов:**
   ```bash
   pm2 status
   systemctl status nginx
   ```

3. **Проверьте логи:**
   ```bash
   pm2 logs invest2026
   tail -f /var/log/nginx/error.log
   ```

### Ошибка 502 Bad Gateway:

```bash
pm2 restart invest2026
pm2 logs invest2026
```

### Ошибка подключения к БД:

Проверьте `.env.production`:
```bash
cat /var/www/invest2026/.env.production
```

### Проблемы с SSL:

```bash
certbot renew --force-renewal
systemctl restart nginx
```

---

## 📚 Документация

- `БЫСТРЫЙ_СТАРТ_VPS.md` - краткая инструкция
- `НАСТРОЙКА_ДОМЕНА_НА_VPS.md` - подробная инструкция
- `vps-setup.sh` - скрипт автоматической установки
- `vps-update.sh` - скрипт обновления сайта
- `НАЧНИТЕ_ЗДЕСЬ_VPS.txt` - быстрая шпаргалка

---

## 💡 Рекомендации

### Для продакшена:

1. **Измените секретные ключи** в `.env.production`:
   ```bash
   nano /var/www/invest2026/.env.production
   ```

2. **Настройте регулярные бэкапы** базы данных

3. **Настройте мониторинг:**
   ```bash
   pm2 install pm2-logrotate
   ```

4. **Обновляйте систему:**
   ```bash
   apt update && apt upgrade -y
   ```

---

## 🎉 Готово!

Ваш Next.js сайт работает на VPS с вашим доменом и SSL!

**Админ панель:**
- URL: `https://ваш-домен.com/admin`
- Логин: `admin`
- Пароль: `X12345x`

**Нужна помощь?** Скажите на каком этапе возникли проблемы!
