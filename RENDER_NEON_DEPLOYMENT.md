# 🚀 Деплой на Render + Neon Database

## Пошаговая инструкция по развертыванию

### Шаг 1: Создание базы данных в Neon (5 минут)

1. **Перейдите на** https://neon.tech
2. **Нажмите "Sign Up"** (можно через GitHub)
3. **Создайте новый проект:**
   - Нажмите "Create Project"
   - Название: `invest2025`
   - Region: выберите ближайший (Europe/US)
   - Postgres Version: 16 (последняя)
4. **Скопируйте Connection String:**
   - После создания проекта вы увидите строку подключения
   - Формат: `postgresql://user:password@host/database?sslmode=require`
   - **СОХРАНИТЕ ЭТУ СТРОКУ** - она понадобится для Render

5. **Выполните SQL для создания таблиц:**
   - В Neon Console откройте "SQL Editor"
   - Скопируйте содержимое файла `complete-database-setup.sql`
   - Вставьте в редактор и нажмите "Run"
   - Дождитесь завершения (все таблицы будут созданы)

---

### Шаг 2: Подготовка проекта (2 минуты)

Все файлы уже готовы! Проверьте наличие:
- ✅ `render.yaml` - конфигурация для Render
- ✅ `package.json` - зависимости проекта
- ✅ `.gitignore` - игнорируемые файлы

---

### Шаг 3: Деплой на Render (5 минут)

#### 3.1 Загрузите код на GitHub (если еще не загружен)

```bash
cd Invest2025-main
git init
git add .
git commit -m "Initial commit for Render deployment"
git branch -M main
git remote add origin https://github.com/ваш-username/invest2025.git
git push -u origin main
```

#### 3.2 Создайте Web Service на Render

1. **Перейдите на** https://render.com
2. **Нажмите "Sign Up"** (можно через GitHub)
3. **Нажмите "New +"** → **"Web Service"**
4. **Подключите GitHub репозиторий:**
   - Выберите ваш репозиторий `invest2025`
   - Нажмите "Connect"

5. **Настройте параметры:**
   ```
   Name: invest2025
   Region: Frankfurt (EU Central) или Oregon (US West)
   Branch: main
   Root Directory: (оставьте пустым)
   Runtime: Node
   Build Command: npm install && npm run build
   Start Command: npm start
   Instance Type: Free
   ```

6. **Добавьте Environment Variables:**
   
   Нажмите "Advanced" → "Add Environment Variable"
   
   Добавьте следующие переменные:

   ```
   DATABASE_URL = ваша_строка_подключения_из_Neon
   POSTGRES_URL = ваша_строка_подключения_из_Neon
   POSTGRES_URL_NON_POOLING = ваша_строка_подключения_из_Neon
   JWT_SECRET = your-super-secret-jwt-key-change-this-in-production
   NEXTAUTH_SECRET = your-super-secret-nextauth-key-change-this
   NEXTAUTH_URL = https://invest2025.onrender.com
   NODE_ENV = production
   ```

   **ВАЖНО:** Замените `ваша_строка_подключения_из_Neon` на реальную строку из Neon!

7. **Нажмите "Create Web Service"**

---

### Шаг 4: Ожидание деплоя (5-10 минут)

Render автоматически:
1. Клонирует ваш репозиторий
2. Установит зависимости (`npm install`)
3. Соберет проект (`npm run build`)
4. Запустит сервер (`npm start`)

Следите за логами в реальном времени на странице деплоя.

---

### Шаг 5: Проверка работы

После успешного деплоя:

1. **Откройте ваш сайт:**
   - URL будет вида: `https://invest2025.onrender.com`
   - Или ваш кастомный домен

2. **Проверьте главную страницу:**
   - Должна загрузиться без ошибок
   - Статистика должна отображаться

3. **Войдите в админ панель:**
   - Перейдите на `/login`
   - Логин: `admin`
   - Пароль: `X12345x`

4. **Проверьте API эндпоинты:**
   - https://invest2025.onrender.com/api/statistics
   - https://invest2025.onrender.com/api/investment-plans
   - https://invest2025.onrender.com/api/settings/site

---

## 🎯 Преимущества Render + Neon

### Render (бесплатный план):
- ✅ 750 часов в месяц (достаточно для одного сервиса 24/7)
- ✅ Автоматический SSL сертификат
- ✅ Автоматические деплои при push в GitHub
- ✅ Логи в реальном времени
- ⚠️ Засыпает после 15 минут неактивности (первый запрос будет медленным)

### Neon (бесплатный план):
- ✅ 0.5 GB хранилища
- ✅ Автоматическое масштабирование
- ✅ Автоматические бэкапы
- ✅ Branching для тестирования
- ✅ Не засыпает (в отличие от Render)

---

## 🔧 Настройка автоматических деплоев

После первого деплоя каждый push в GitHub будет автоматически деплоить изменения:

```bash
# Внесите изменения в код
git add .
git commit -m "Update feature"
git push

# Render автоматически задеплоит изменения!
```

---

## 🌐 Настройка кастомного домена (опционально)

1. В Render Dashboard откройте ваш сервис
2. Перейдите в "Settings" → "Custom Domain"
3. Добавьте ваш домен (например, `invest2025.com`)
4. Настройте DNS записи у вашего регистратора:
   ```
   Type: CNAME
   Name: @
   Value: invest2025.onrender.com
   ```

---

## 📊 Мониторинг и логи

### Просмотр логов:
1. Откройте ваш сервис в Render Dashboard
2. Перейдите в "Logs"
3. Смотрите логи в реальном времени

### Метрики:
- CPU usage
- Memory usage
- Request count
- Response time

---

## 🆘 Решение проблем

### Проблема: Сайт не открывается
**Решение:** Проверьте логи в Render Dashboard. Возможно, ошибка в переменных окружения.

### Проблема: API возвращает ошибки базы данных
**Решение:** 
1. Проверьте, что `DATABASE_URL` правильно указан
2. Убедитесь, что SQL скрипт был выполнен в Neon
3. Проверьте, что в строке подключения есть `?sslmode=require`

### Проблема: Сайт засыпает
**Решение:** Это нормально для бесплатного плана Render. Первый запрос после 15 минут неактивности будет медленным (cold start ~30 секунд).

Для решения можно:
- Использовать сервис пингования (например, UptimeRobot)
- Перейти на платный план Render ($7/месяц)

### Проблема: Build failed
**Решение:**
1. Проверьте, что `package.json` содержит все зависимости
2. Убедитесь, что локально `npm run build` работает без ошибок
3. Проверьте версию Node.js (должна быть 18+)

---

## 💰 Стоимость

### Бесплатно навсегда:
- Render Free Plan: $0/месяц
- Neon Free Plan: $0/месяц

### Если нужно больше:
- Render Starter: $7/месяц (без засыпания)
- Neon Pro: от $19/месяц (больше хранилища)

---

## ✅ Чеклист готовности

- [ ] База данных создана в Neon
- [ ] SQL скрипт выполнен (таблицы созданы)
- [ ] Код загружен на GitHub
- [ ] Web Service создан на Render
- [ ] Environment Variables добавлены
- [ ] Деплой завершен успешно
- [ ] Сайт открывается
- [ ] Вход в админ панель работает
- [ ] API эндпоинты отвечают

---

## 🎉 Готово!

Ваш сайт теперь доступен в интернете 24/7!

**URL:** https://invest2025.onrender.com
**Админ:** admin / X12345x

Наслаждайтесь! 🚀
