# 🚀 АВТОМАТИЧЕСКАЯ УСТАНОВКА НА VPS

## ✅ Всё готово к установке!

Я создал автоматический скрипт, который установит всё за вас.

---

## 📋 ШАГ 1: Подключитесь к VPS

### Windows:
Дважды кликните на файл:
```
vps-connect.bat
```

Или в PowerShell:
```powershell
ssh root@130.49.213.197
```

Введите пароль от VPS когда попросит.

---

## 📋 ШАГ 2: Запустите автоматическую установку

После подключения к VPS выполните эти команды:

```bash
# Скачиваем скрипт установки
curl -o install.sh https://raw.githubusercontent.com/KHUDOYDOD/invest2026/main/vps-auto-install.sh

# Даём права на выполнение
chmod +x install.sh

# Запускаем установку
./install.sh
```

**ИЛИ** скопируйте скрипт вручную:

```bash
# Создаём файл
nano install.sh
```

Скопируйте содержимое файла `vps-auto-install.sh` из вашего проекта.

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

Запустите:
```bash
chmod +x install.sh
./install.sh
```

---

## ⏱️ Процесс установки (10-15 минут)

Скрипт автоматически:
1. ✅ Обновит систему
2. ✅ Установит Node.js 20.x
3. ✅ Установит PM2
4. ✅ Установит Nginx
5. ✅ Установит Git
6. ✅ Склонирует ваш проект
7. ✅ Создаст .env файл
8. ✅ Установит зависимости
9. ✅ Соберёт проект
10. ✅ Запустит приложение
11. ✅ Настроит автозапуск
12. ✅ Настроит Nginx
13. ✅ Настроит файрвол

---

## 🎉 ШАГ 3: Проверьте работу

После завершения установки откройте в браузере:

```
http://130.49.213.197
```

Ваше приложение должно работать! 🚀

---

## 📝 Полезные команды

### Просмотр логов:
```bash
pm2 logs investpro
```

### Перезапуск приложения:
```bash
pm2 restart investpro
```

### Статус приложения:
```bash
pm2 status
```

### Обновление из GitHub:
```bash
cd /root/invest2026
git pull
npm install
npm run build
pm2 restart investpro
```

**ИЛИ** используйте готовый bat-файл:
```
vps-update.bat
```

---

## 🔧 Если что-то пошло не так

### Проверить логи установки:
```bash
cat /var/log/syslog | tail -100
```

### Проверить статус Nginx:
```bash
systemctl status nginx
```

### Проверить порты:
```bash
netstat -tulpn | grep :3000
netstat -tulpn | grep :80
```

### Перезапустить всё:
```bash
pm2 restart investpro
systemctl restart nginx
```

---

## 🌐 Подключение домена (опционально)

Если хотите подключить свой домен:

1. В настройках домена добавьте A-запись:
   ```
   @ -> 130.49.213.197
   www -> 130.49.213.197
   ```

2. Обновите конфигурацию Nginx:
   ```bash
   nano /etc/nginx/sites-available/investpro
   # Измените server_name на ваш домен
   ```

3. Установите SSL:
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
   ```

4. Обновите NEXTAUTH_URL:
   ```bash
   cd /root/invest2026
   nano .env.production
   # Измените NEXTAUTH_URL на https://ваш-домен.ru
   pm2 restart investpro
   ```

---

## 📊 Что установлено

- **Node.js**: 20.x
- **PM2**: Менеджер процессов
- **Nginx**: Веб-сервер (порт 80)
- **Next.js**: Ваше приложение (порт 3000)
- **UFW**: Файрвол
- **Git**: Система контроля версий

---

## 🎯 Следующие шаги

1. ✅ Откройте http://130.49.213.197
2. ✅ Проверьте работу сайта
3. ✅ Зайдите в админку
4. ✅ Настройте тарифы и статистику
5. ✅ (Опционально) Подключите домен

---

## 💡 Совет

Сохраните эти файлы:
- `vps-connect.bat` - для быстрого подключения
- `vps-update.bat` - для быстрого обновления
- `vps-auto-install.sh` - скрипт установки

---

## 🆘 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи: `pm2 logs investpro`
2. Проверьте статус: `pm2 status`
3. Перезапустите: `pm2 restart investpro`

---

## ✅ ГОТОВО!

Ваше приложение работает на VPS! 🎉
