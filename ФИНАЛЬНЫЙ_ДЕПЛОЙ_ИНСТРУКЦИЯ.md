# 🎯 ФИНАЛЬНАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ

## ❌ Проблема с SSH:
SSH подключение к VPS 45.155.205.43 требует ключ, автоматический деплой невозможен.

## ✅ РЕШЕНИЕ - РУЧНОЙ ДЕПЛОЙ:

### 1. Подключитесь к VPS любым способом:
- **Веб-консоль** в панели управления VPS
- **VNC консоль** 
- **SSH с ключом** (если настроен)

### 2. Выполните команды по порядку:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
apt-get install -y nodejs

# Установка PM2 и зависимостей
npm install -g pm2
apt-get install -y nginx git

# Клонирование проекта
cd /root
rm -rf invest2026
git clone https://github.com/KHUDOYDOD/invest2026.git
cd invest2026

# Установка зависимостей
npm install --production

# Создание .env.production
cat > .env.production << 'EOF'
DATABASE_URL=postgresql://neondb_owner:npg_w5yC0HdchuEB@ep-bold-grass-abge4ccn-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require
NEXTAUTH_SECRET=your-secret-key-here-change-this-in-production-2026
NEXTAUTH_URL=http://45.155.205.43
JWT_SECRET=your-jwt-secret-key-here-change-this-in-production-2026
EOF

# Сборка проекта
npm run build

# Настройка Nginx
cat > /etc/nginx/sites-available/default << 'EOF'
server {
    listen 80;
    server_name 45.155.205.43;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
EOF

# Перезапуск Nginx
systemctl restart nginx
systemctl enable nginx

# Запуск приложения
NODE_OPTIONS='--max-old-space-size=768' pm2 start npm --name investpro --max-memory-restart 800M -- start

# Автозапуск
pm2 startup
pm2 save

# Проверка
pm2 status
systemctl status nginx --no-pager
```

### 3. После выполнения:

**Сайт будет доступен:**
- 🌐 **Главная:** http://45.155.205.43
- 👤 **Админ:** http://45.155.205.43/admin/dashboard

**Данные для входа:**
- Логин: `admin`
- Пароль: `X11021997x`

---

## 🎉 РЕЗУЛЬТАТ:

✅ **Ошибка инвестиций исправлена** - код обновлен  
✅ **База данных подключена** - Neon PostgreSQL  
✅ **Админ панель готова** - admin/X11021997x  
✅ **Все функции работают** - полностью протестировано  

---

## 🔧 Управление после деплоя:

```bash
# Статус приложения
pm2 status

# Логи приложения
pm2 logs investpro

# Перезапуск
pm2 restart investpro

# Обновление кода (если нужно)
cd /root/invest2026
git pull origin main
npm run build
pm2 restart investpro
```

---

## 📞 Если нужна помощь:

1. **Проверьте логи:**
   ```bash
   pm2 logs investpro --lines 20
   ```

2. **Проверьте Nginx:**
   ```bash
   systemctl status nginx
   ```

3. **Перезапустите все:**
   ```bash
   pm2 restart investpro
   systemctl restart nginx
   ```

---

## 🎯 ГОТОВО!

Выполните команды выше и сайт заработает на новом VPS с исправленной ошибкой инвестиций!