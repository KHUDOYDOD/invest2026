# 🔧 Исправление редактирования профиля

## Проблема
Не работает редактирование полей профиля (телефон, страна, город).

## Причина
Возможно, в таблице `users` отсутствует поле `city`.

## Решение

### Шаг 1: Добавьте поле city в базу данных

Запустите bat-файл:
```
add-city-field.bat
```

Или вручную выполните SQL:
```
psql "%DATABASE_URL%" -f add-city-field.sql
```

### Шаг 2: Перезапустите сервер

```
npm run dev
```

### Шаг 3: Проверьте редактирование

1. Откройте страницу профиля: http://localhost:3000/dashboard/profile
2. Нажмите кнопку **"Редактировать"** в правом верхнем углу
3. Поля станут редактируемыми (с синей рамкой при фокусе)
4. Измените данные:
   - Полное имя
   - Телефон
   - Страна
   - Город
5. Нажмите **"Сохранить"** (зеленая кнопка)
6. Данные сохранятся в базе данных

## Что было исправлено

### 1. Улучшен интерфейс редактирования
- ✅ Кнопка "Редактировать" теперь четко переключает режим
- ✅ В режиме редактирования появляются две кнопки: "Сохранить" и "Отмена"
- ✅ Поля ввода имеют синюю подсветку при фокусе
- ✅ Добавлены placeholder'ы для пустых полей

### 2. Добавлена кнопка "Отмена"
- При нажатии на "Отмена" все изменения отменяются
- Данные возвращаются к исходным значениям

### 3. Улучшено визуальное отображение
- В режиме просмотра поля имеют светлый фон
- В режиме редактирования поля становятся интерактивными
- Четкое разделение между режимами

## Проверка работы

### До редактирования:
```
Полное имя: sasa1
Email: sasa@sasa.sasa
Телефон: Не указан
Страна: Не указана
Город: Не указан
```

### После редактирования:
```
Полное имя: Иван Иванов
Email: sasa@sasa.sasa (не редактируется)
Телефон: +7 (999) 123-45-67
Страна: Россия
Город: Москва
```

## API Endpoint

Обновление профиля использует:
```
PUT /api/user/profile
```

Тело запроса:
```json
{
  "userId": "user-uuid",
  "full_name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67",
  "country": "Россия",
  "city": "Москва"
}
```

## Поля в базе данных

Таблица `users` должна содержать:
- `full_name` VARCHAR(255) - полное имя
- `phone` VARCHAR(50) - телефон
- `country` VARCHAR(100) - страна
- `city` VARCHAR(100) - город ⚠️ **Добавляется скриптом**

## Если проблема не решена

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте, что поле `city` добавлено в базу данных:
   ```sql
   SELECT column_name FROM information_schema.columns 
   WHERE table_name = 'users' AND column_name = 'city';
   ```
3. Проверьте, что API endpoint `/api/user/profile` работает
4. Убедитесь, что `userId` сохранен в localStorage

## Готово! 🎉

Теперь вы можете редактировать все поля профиля, и изменения будут сохраняться в базе данных.
