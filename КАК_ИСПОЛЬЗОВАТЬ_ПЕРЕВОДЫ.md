# 📖 Как использовать систему переводов

## ✅ Что готово

Система мультиязычности полностью настроена и готова к использованию!

- ✅ 6 языков: Русский, English, Қазақша, Тоҷикӣ, Кыргызча, O'zbekcha
- ✅ Переключатель языков в Header
- ✅ Автосохранение выбранного языка
- ✅ Все базовые переводы готовы

## 🎯 Как добавить переводы в компонент

### 1. Откройте компонент

Например, `components/hero-section.tsx`

### 2. Добавьте импорт

```tsx
import { useLanguage } from '@/contexts/language-context'
```

### 3. Используйте хук

```tsx
export function HeroSection() {
  const { t } = useLanguage()  // Добавьте эту строку
  
  // Остальной код...
}
```

### 4. Замените текст

**Было:**
```tsx
<h1>Инвестируйте с умом, получайте стабильный доход</h1>
```

**Стало:**
```tsx
<h1>{t('hero.title')}</h1>
```

## 📝 Список ключей для разных разделов

### Главная страница
- `hero.title` - Заголовок баннера
- `hero.subtitle` - Подзаголовок
- `hero.cta_primary` - Кнопка "Начать инвестировать"
- `hero.stats_users` - "Активных инвесторов"
- `hero.stats_invested` - "Общие инвестиции"

### Статистика
- `statistics.title` - "Наши достижения"
- `statistics.users` - "Активные инвесторы"
- `statistics.investments` - "Месячные инвестиции"

### Тарифы
- `plans.title` - "Инвестиционные планы"
- `plans.min_deposit` - "Минимальный депозит"
- `plans.daily_profit` - "Ежедневная прибыль"
- `plans.choose` - "Выбрать план"

### Личный кабинет
- `dashboard.balance` - "Баланс"
- `dashboard.deposit` - "Пополнить"
- `dashboard.withdraw` - "Вывести"

### Кнопки и общее
- `common.save` - "Сохранить"
- `common.cancel` - "Отмена"
- `common.loading` - "Загрузка..."

## 🔍 Где найти все ключи

Откройте файл: `lib/i18n/locales/ru.json`

Там все ключи с русскими переводами. Просто скопируйте нужный ключ.

## 💡 Примеры

### Пример 1: Простая замена

```tsx
// Было
<button>Войти</button>

// Стало
<button>{t('header.login')}</button>
```

### Пример 2: С переменными

```tsx
// Было
<p>Баланс: ${balance}</p>

// Стало
<p>{t('dashboard.balance')}: ${balance}</p>
```

### Пример 3: В атрибутах

```tsx
// Было
<input placeholder="Введите email" />

// Стало
<input placeholder={t('auth.email')} />
```

## 🚀 Быстрый старт

1. Откройте любой компонент
2. Добавьте `import { useLanguage } from '@/contexts/language-context'`
3. Добавьте `const { t } = useLanguage()`
4. Замените текст на `{t('ключ')}`
5. Готово! 🎉

## 📞 Нужна помощь?

Если не знаете какой ключ использовать:
1. Откройте `lib/i18n/locales/ru.json`
2. Найдите нужный текст
3. Скопируйте ключ (например, `hero.title`)
4. Используйте как `{t('hero.title')}`

---

**Все работает!** Просто начните использовать `t()` вместо обычного текста! 🌍
