# 🎮 УПРАВЛЕНИЕ VPS - БЫСТРЫЕ КОМАНДЫ

## 📁 Созданные файлы для управления:

### 🔌 Подключение
**vps-connect.bat** - Подключиться к VPS через SSH
```
Двойной клик → Введите пароль
```

---

### 🔄 Обновление сайта
**update_site.bat** - Автоматическое обновление из GitHub
```
Двойной клик → Автоматически:
  1. Скачает изменения из GitHub
  2. Установит зависимости
  3. Соберёт проект
  4. Перезапустит приложение
```

**ИЛИ вручную:**
```
update_site.ps1
```

---

### 📊 Мониторинг

**vps-status.bat** - Показать статус приложения
```
Двойной клик → Покажет:
  - Статус PM2
  - Использование диска
  - Использование памяти
```

**vps-logs.bat** - Просмотр логов в реальном времени
```
Двойной клик → Показывает последние 50 строк логов
Ctrl+C для выхода
```

---

### 🔧 Управление

**vps-restart.bat** - Перезапустить приложение
```
Двойной клик → Перезапускает приложение
```

---

## 📝 Команды SSH (если подключены через vps-connect.bat)

### Просмотр логов:
```bash
pm2 logs investpro
pm2 logs investpro --lines 100
```

### Статус приложения:
```bash
pm2 status
```

### Перезапуск:
```bash
pm2 restart investpro
```

### Остановка:
```bash
pm2 stop investpro
```

### Запуск:
```bash
pm2 start investpro
```

### Обновление из GitHub:
```bash
cd /root/invest2026
git pull
npm install --production
npm run build
pm2 restart investpro
```

### Проверка места на диске:
```bash
df -h
```

### Проверка использования памяти:
```bash
free -h
```

### Очистка места:
```bash
apt autoremove -y
apt clean
npm cache clean --force
journalctl --vacuum-time=3d
```

---

## 🌐 Доступ к сайту

**URL:** http://130.49.213.197

---

## 🆘 Решение проблем

### Сайт не открывается:
```bash
# Проверьте статус
pm2 status

# Проверьте логи
pm2 logs investpro --lines 50

# Перезапустите
pm2 restart investpro

# Проверьте Nginx
systemctl status nginx
systemctl restart nginx
```

### Нет места на диске:
```bash
# Очистка
apt autoremove -y
apt clean
npm cache clean --force
journalctl --vacuum-time=3d

# Проверка
df -h
```

### Приложение падает:
```bash
# Смотрим логи
pm2 logs investpro --lines 100

# Проверяем .env файл
cat /root/invest2026/.env.production

# Перезапускаем
pm2 restart investpro
```

---

## 📦 Структура файлов

```
📁 Ваш проект/
├── 🔌 vps-connect.bat          # Подключение к VPS
├── 🔄 update_site.bat          # Обновление сайта (BAT)
├── 🔄 update_site.ps1          # Обновление сайта (PowerShell)
├── 📊 vps-status.bat           # Статус приложения
├── 📝 vps-logs.bat             # Просмотр логов
├── 🔧 vps-restart.bat          # Перезапуск приложения
├── 🚀 vps-auto-install.sh      # Полная установка
├── ⚡ vps-install-light.sh     # Облегчённая установка
├── 🧹 vps-cleanup.sh           # Очистка места
└── 📖 УПРАВЛЕНИЕ_VPS.md        # Эта инструкция
```

---

## ✨ Быстрый старт

1. **Первая установка:**
   - Откройте `vps-connect.bat`
   - Выполните команду установки из `НАЧАТЬ_УСТАНОВКУ_VPS.md`

2. **Обновление сайта:**
   - Откройте `update_site.bat`
   - Готово!

3. **Проверка работы:**
   - Откройте `vps-status.bat`
   - Или откройте http://130.49.213.197

4. **Просмотр логов:**
   - Откройте `vps-logs.bat`

---

## 🎯 Рекомендации

- Делайте обновления через `update_site.bat` - это безопасно
- Проверяйте логи после обновления: `vps-logs.bat`
- Регулярно проверяйте место на диске: `vps-status.bat`
- Делайте бэкапы базы данных (Neon делает автоматически)

---

## 📞 Поддержка

Если что-то не работает:
1. Откройте `vps-logs.bat` и посмотрите ошибки
2. Откройте `vps-status.bat` и проверьте ресурсы
3. Попробуйте `vps-restart.bat`
4. Если не помогло - подключитесь через `vps-connect.bat` и проверьте вручную

---

## ✅ Готово!

Теперь у вас есть полный набор инструментов для управления VPS! 🎉
