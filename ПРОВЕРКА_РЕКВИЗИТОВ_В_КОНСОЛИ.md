# 🔍 Проверка реквизитов в консоли

## Что мы знаем

✅ В базе данных реквизиты ЕСТЬ:
- `card_number: 8686787878787879`
- `card_holder_name: kjhkj hbhgbgbj j`

❌ Но в админ-панели показывает "Реквизиты не указаны"

## Что нужно сделать

### 1. Перезапустите сервер

Обязательно перезапустите сервер, чтобы изменения вступили в силу:

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите заново:
npm run dev
```

### 2. Откройте консоль браузера

1. Откройте админ-панель → Заявки
2. Нажмите **F12** (откроется DevTools)
3. Перейдите на вкладку **Console**
4. Нажмите кнопку **"Обновить"** в админ-панели

### 3. Проверьте логи

В консоли должны появиться логи:

```
✅ Withdrawal response data: { success: true, requests: [...] }
✅ Withdrawal requests loaded: 11
📋 First withdrawal request: { id: "...", amount: 244, ... }
🔍 Checking payment details: {
  has_card_number: true,
  has_phone_number: false,
  has_wallet_address: false,
  card_number: "8686787878787879",
  card_holder_name: "kjhkj hbhgbgbj j",
  ...
}
```

### 4. Что искать в логах

#### ✅ Если видите `has_card_number: true` и `card_number: "8686..."`
Значит данные приходят с сервера, но не отображаются.
**Проблема в React компоненте.**

#### ❌ Если видите `has_card_number: false` или `card_number: null`
Значит данные не приходят с сервера.
**Проблема в API.**

### 5. Проверьте логи сервера

В терминале, где запущен `npm run dev`, должно быть:

```
=== ADMIN GET WITHDRAWAL REQUESTS ===
✅ Admin verified: admin@example.com
✅ Found 11 withdrawal requests
📋 First request with card details: {
  id: "8231b2d5-5669-4131-9765-c870b73a98f1",
  card_number: "8686787878787879",
  card_holder_name: "kjhkj hbhgbgbj j"
}
```

## Возможные проблемы

### Проблема 1: Сервер не перезапущен
**Решение:** Остановите (Ctrl+C) и запустите заново `npm run dev`

### Проблема 2: Кэш браузера
**Решение:** 
1. Нажмите Ctrl+Shift+R (жесткая перезагрузка)
2. Или очистите кэш: F12 → Network → Disable cache

### Проблема 3: Данные не приходят с API
**Решение:** Проверьте логи сервера на ошибки SQL

### Проблема 4: React не обновляет компонент
**Решение:** Проверьте, что `withdrawalRequests` обновляется в state

## Следующие шаги

После проверки логов:

1. **Если данные приходят** (has_card_number: true):
   - Проблема в отображении React компонента
   - Нужно проверить условия `{request.card_number && ...}`

2. **Если данные НЕ приходят** (has_card_number: false):
   - Проблема в SQL запросе или форматировании
   - Нужно проверить API route

---

**Сделайте проверку и покажите мне логи из консоли браузера!**
