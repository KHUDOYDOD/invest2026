# ✅ Исправлено: Реквизиты в заявках на вывод

## Проблема
Пользователь указывал реквизиты при выводе средств (номер карты, ФИО, телефон для СБП), но в админ-панели показывалось "Реквизиты не указаны".

## Что было исправлено

### 1. База данных
Добавлены новые поля в таблицу `withdrawal_requests`:
- `card_number` - номер банковской карты
- `card_holder_name` - ФИО владельца карты
- `phone_number` - номер телефона для СБП
- `account_holder_name` - ФИО владельца счета (СБП)
- `crypto_network` - сеть криптовалюты (trc20, ton)

### 2. API для создания заявки (`/api/withdraw`)
Обновлен для сохранения всех реквизитов:
- Принимает все поля с реквизитами
- Сохраняет их в соответствующие колонки БД

### 3. Страница вывода средств (`/dashboard/withdraw`)
Обновлена для отправки всех данных:
- Отправляет номер карты и ФИО владельца (для карт)
- Отправляет телефон и ФИО (для СБП)
- Отправляет адрес кошелька и сеть (для крипты)

### 4. API для админа (`/api/admin/withdrawal-requests`)
Обновлен для получения всех реквизитов из БД

### 5. Админ-панель (`/admin/requests`)
Обновлена для отображения всех реквизитов:
- **Для карт**: показывает номер карты и ФИО владельца
- **Для СБП**: показывает телефон и ФИО владельца
- **Для крипты**: показывает сеть и адрес кошелька

## Как применить исправления

### Шаг 1: Обновить базу данных
Запустите скрипт для добавления новых полей:

```bash
add-withdrawal-details.bat
```

Или вручную выполните SQL:
```sql
-- Добавляем поля для реквизитов
ALTER TABLE withdrawal_requests 
ADD COLUMN IF NOT EXISTS card_number VARCHAR(19),
ADD COLUMN IF NOT EXISTS card_holder_name VARCHAR(255),
ADD COLUMN IF NOT EXISTS phone_number VARCHAR(20),
ADD COLUMN IF NOT EXISTS account_holder_name VARCHAR(255),
ADD COLUMN IF NOT EXISTS crypto_network VARCHAR(50);

-- Делаем wallet_address необязательным
ALTER TABLE withdrawal_requests 
ALTER COLUMN wallet_address DROP NOT NULL;
```

### Шаг 2: Перезапустить сервер
После обновления БД перезапустите сервер разработки:

```bash
npm run dev
```

## Проверка работы

### Для пользователя:
1. Зайдите в раздел "Вывод средств"
2. Выберите способ вывода (карта/СБП/крипта)
3. Заполните все реквизиты
4. Создайте заявку

### Для администратора:
1. Зайдите в админ-панель → Заявки
2. Откройте вкладку "Выводы"
3. Теперь вы увидите все реквизиты пользователя:
   - **Карта**: номер карты + ФИО владельца
   - **СБП**: телефон + ФИО владельца
   - **Крипта**: сеть + адрес кошелька

## Пример отображения в админ-панели

### Для банковской карты:
```
💳 Номер карты: 1234 5678 9012 3456
👤 Владелец карты: Иванов Иван Иванович
```

### Для СБП:
```
📱 Телефон (СБП): +7 (999) 123-45-67
👤 Владелец: Иванов Иван Иванович
```

### Для криптовалюты:
```
🌐 Сеть: TRC20
🔐 Адрес кошелька: TXyz123...abc789
```

## Файлы, которые были изменены

1. `scripts/add-withdrawal-details-fields.sql` - SQL скрипт для БД
2. `add-withdrawal-details.bat` - батник для запуска скрипта
3. `app/api/withdraw/route.ts` - API создания заявки
4. `app/dashboard/withdraw/page.tsx` - страница вывода
5. `app/api/admin/withdrawal-requests/route.ts` - API для админа
6. `app/admin/requests/page.tsx` - админ-панель

## Готово! 🎉

Теперь все реквизиты пользователя сохраняются и отображаются в админ-панели.
