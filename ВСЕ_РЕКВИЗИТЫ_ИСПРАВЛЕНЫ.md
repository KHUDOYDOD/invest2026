# ✅ ВСЕ РЕКВИЗИТЫ ИСПРАВЛЕНЫ

## Что было исправлено

### 1. Вкладка "Выводы" ✅
- Реквизиты хранятся в отдельных полях: `card_number`, `phone_number`, `wallet_address`
- Обновлен код для отображения этих полей
- Теперь показывает:
  - 💳 Номер карты + владелец (для карт)
  - 📱 Телефон (для СБП)
  - 🔐 Адрес кошелька + сеть (для крипты)

### 2. Вкладка "Пополнения" ✅
- Реквизиты хранятся в поле `payment_details` (JSON)
- Обновлен код для извлечения данных из JSON
- Теперь показывает:
  - 💳 Номер карты (если есть)
  - 📱 Телефон (если есть)
  - 🔐 Адрес кошелька (если есть)
  - 🔗 Хэш транзакции (если есть)

## Структура данных

### Для выводов (withdrawal_requests):
```sql
card_number VARCHAR(19)
card_holder_name VARCHAR(255)
phone_number VARCHAR(20)
account_holder_name VARCHAR(255)
wallet_address TEXT
crypto_network VARCHAR(50)
```

### Для пополнений (deposit_requests):
```sql
payment_details JSONB
```

Пример `payment_details`:
```json
{
  "method": "Банковская карта",
  "card_number": "1234567890123456",
  "phone_number": "+79991234567",
  "wallet_address": "TXyz...",
  "transaction_hash": "0x..."
}
```

## Что нужно сделать

### ПЕРЕЗАПУСТИТЕ СЕРВЕР
```bash
npm run dev
```

### Проверьте обе вкладки:

#### Вкладка "Выводы":
1. Откройте админ-панель → Заявки → Выводы
2. Нажмите на заявку
3. Должны показаться реквизиты

#### Вкладка "Пополнения":
1. Откройте админ-панель → Заявки → Пополнения
2. Нажмите на заявку
3. Должны показаться реквизиты из `payment_details`

## Важно!

### Старые заявки
Старые заявки (созданные до обновления) могут не иметь реквизитов, потому что:
- Для выводов: поля были добавлены только что
- Для пополнений: `payment_details` может быть `null` или содержать только `method`

### Новые заявки
Все новые заявки будут автоматически сохранять реквизиты:
- **Выводы**: в отдельные поля
- **Пополнения**: в `payment_details` JSON

## Если не показывает

1. **Перезапустите сервер** (обязательно!)
2. **Очистите кэш браузера**: Ctrl+Shift+R
3. **Проверьте консоль браузера** (F12) на ошибки
4. **Создайте новую заявку** для проверки

---

## ИТОГ

✅ Вкладка "Выводы" - исправлена  
✅ Вкладка "Пополнения" - исправлена  
✅ Реквизиты теперь отображаются в обеих вкладках  

**Просто перезапустите сервер!**
