# ⚡ БЫСТРЫЙ СТАРТ - НАСТРОЙКА ДОМЕНА НА VPS

## 🎯 Что нужно:

- ✅ VPS сервер (Ubuntu/Debian)
- ✅ Свой домен
- ✅ SSH доступ к VPS

---

## 🚀 АВТОМАТИЧЕСКАЯ УСТАНОВКА (5 минут)

### 1. Подключитесь к VPS:
```bash
ssh root@IP_ВАШЕГО_VPS
```

### 2. Скачайте и запустите скрипт:
```bash
wget https://raw.githubusercontent.com/KHUDOYDOD/invest2026/main/vps-setup.sh
bash vps-setup.sh
```

Скрипт автоматически:
- ✅ Установит Node.js, PM2, Nginx, Certbot
- ✅ Клонирует проект
- ✅ Настроит окружение
- ✅ Соберет и запустит сайт
- ✅ Настроит Nginx и Firewall

### 3. Настройте DNS:

В панели управления доменом добавьте:

```
Тип: A
Имя: @
Значение: IP_ВАШЕГО_VPS
```

```
Тип: A
Имя: www
Значение: IP_ВАШЕГО_VPS
```

### 4. Подождите 15-30 минут (пока DNS обновится)

### 5. Установите SSL:
```bash
certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

## ✅ ГОТОВО!

Сайт доступен: `https://ваш-домен.com`

---

## 🔄 ОБНОВЛЕНИЕ САЙТА

Когда нужно обновить код:

```bash
ssh root@IP_ВАШЕГО_VPS
bash /var/www/invest2026/vps-update.sh
```

Или вручную:
```bash
cd /var/www/invest2026
git pull origin main
npm run build
pm2 restart invest2026
```

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Просмотр логов
pm2 logs invest2026

# Перезапуск сайта
pm2 restart invest2026

# Статус
pm2 status

# Перезапуск Nginx
systemctl restart nginx

# Просмотр логов Nginx
tail -f /var/log/nginx/error.log
```

---

## 📋 РУЧНАЯ УСТАНОВКА

Если автоматический скрипт не работает, следуйте инструкции:
`НАСТРОЙКА_ДОМЕНА_НА_VPS.md`

---

## 🆘 ПРОБЛЕМЫ?

### Сайт не открывается:

1. **Проверьте DNS:**
   ```bash
   nslookup ваш-домен.com
   ```

2. **Проверьте статус:**
   ```bash
   pm2 status
   systemctl status nginx
   ```

3. **Проверьте логи:**
   ```bash
   pm2 logs invest2026
   tail -f /var/log/nginx/error.log
   ```

### Ошибка 502:
```bash
pm2 restart invest2026
```

### Ошибка SSL:
```bash
certbot renew --force-renewal
```

---

## 📞 Нужна помощь?

Скажите на каком этапе возникла проблема, помогу разобраться!
