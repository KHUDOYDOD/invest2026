# 🚨 ИНСТРУКЦИЯ ПО ВОССТАНОВЛЕНИЮ САЙТА

## Ситуация:
- ✅ VPS отвечает на ping
- ✅ SSH работает (требует пароль)
- ❌ Сайт показывает 502 Bad Gateway
- ❌ PM2 процесс упал

---

## 🎯 РЕШЕНИЕ:

### Шаг 1: Подключение к VPS

Откройте командную строку и выполните:

```bash
ssh root@130.49.213.197
```

**Пароль:** `12345678`

### Шаг 2: Проверка статуса PM2

```bash
cd /root/invest2026
pm2 status
```

### Шаг 3: Перезапуск PM2

Если процесс `investpro` есть в списке:
```bash
pm2 restart investpro
```

Если процесса нет или он не запускается:
```bash
NODE_OPTIONS='--max-old-space-size=768' pm2 start npm --name investpro --max-memory-restart 800M -- start
```

### Шаг 4: Проверка результата

```bash
pm2 status
pm2 logs investpro --lines 10
```

### Шаг 5: Обновление кода (опционально)

Если нужно загрузить исправленный код:
```bash
git pull origin main
npm run build
pm2 restart investpro
```

---

## 🌐 Проверка сайта:

После выполнения команд откройте:
- **Сайт:** http://130.49.213.197
- **Админ панель:** http://130.49.213.197/admin/dashboard

**Данные для входа в админ:**
- Логин: `admin`
- Пароль: `X11021997x`

---

## 📋 Если проблемы остаются:

1. Проверьте логи:
```bash
pm2 logs investpro --lines 50
```

2. Проверьте свободное место:
```bash
df -h
```

3. Проверьте память:
```bash
free -h
```

4. Перезапустите Nginx:
```bash
sudo systemctl restart nginx
```

---

## ✅ Признаки успешного восстановления:

- PM2 показывает статус `online` для процесса `investpro`
- Сайт http://130.49.213.197 открывается без ошибок
- Админ панель доступна
- **Инвестиции работают без ошибки 500!** 🎉

---

## 🆘 Если ничего не помогает:

Отправьте тикет в поддержку 4VPS с текстом из файла `СРОЧНЫЙ_ТИКЕТ_4VPS.txt`