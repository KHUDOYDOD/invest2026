# 🎯 ЧТО ДЕЛАТЬ СЕЙЧАС - ПРИВЯЗКА ДОМЕНА НА VPS

## ✅ Всё готово для установки!

Я создал автоматические скрипты и инструкции для привязки вашего домена к Next.js на VPS.

---

## 🚀 ДЕЙСТВУЙТЕ ПРЯМО СЕЙЧАС:

### ШАГ 1: Подключитесь к VPS

Откройте терминал (PowerShell/CMD) и выполните:

```bash
ssh root@IP_ВАШЕГО_VPS
```

Замените `IP_ВАШЕГО_VPS` на реальный IP адрес вашего сервера.

---

### ШАГ 2: Запустите автоматическую установку

Скопируйте и выполните эти команды на VPS:

```bash
wget https://raw.githubusercontent.com/KHUDOYDOD/invest2026/main/vps-setup.sh
bash vps-setup.sh
```

Скрипт спросит:
1. **Ваш домен** (например: `mysite.com`)
2. **IP вашего VPS**

Затем автоматически всё настроит (5-10 минут).

---

### ШАГ 3: Настройте DNS

Пока скрипт работает, откройте панель управления доменом и добавьте:

**A-запись для основного домена:**
```
Тип: A
Имя: @ (или оставьте пустым)
Значение: IP_ВАШЕГО_VPS
TTL: 3600
```

**A-запись для www:**
```
Тип: A
Имя: www
Значение: IP_ВАШЕГО_VPS
TTL: 3600
```

**Где настроить DNS:**
- Если домен на Cloudflare: https://dash.cloudflare.com → DNS
- Если на Namecheap: Dashboard → Advanced DNS
- Если на GoDaddy: DNS Management
- Если на другом регистраторе: найдите раздел DNS/Управление DNS

---

### ШАГ 4: Подождите DNS обновления

DNS обновляется от 5 минут до 24 часов (обычно 15-30 минут).

**Проверить DNS можно так:**
```bash
nslookup ваш-домен.com
```

Должен показать IP вашего VPS.

---

### ШАГ 5: Установите SSL сертификат

Когда DNS обновится, выполните на VPS:

```bash
certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

Следуйте инструкциям:
1. Введите email
2. Согласитесь с условиями (Y)
3. Выберите опцию 2 (redirect HTTP to HTTPS)

---

## ✅ ГОТОВО!

Ваш сайт теперь доступен:
- `https://ваш-домен.com`
- `https://www.ваш-домен.com`

**Админ панель:**
- URL: `https://ваш-домен.com/admin`
- Логин: `admin`
- Пароль: `X12345x`

---

## 🔄 Обновление сайта в будущем

Когда нужно обновить код:

```bash
ssh root@IP_VPS
bash /var/www/invest2026/vps-update.sh
```

---

## 📚 Документация

Все инструкции сохранены в проекте:

- **НАЧНИТЕ_ЗДЕСЬ_VPS.txt** - быстрая шпаргалка
- **БЫСТРЫЙ_СТАРТ_VPS.md** - краткая инструкция
- **НАСТРОЙКА_ДОМЕНА_НА_VPS.md** - подробная инструкция
- **README_VPS_ДОМЕН.md** - полное руководство
- **vps-setup.sh** - скрипт автоматической установки
- **vps-update.sh** - скрипт обновления

---

## 🆘 Проблемы?

### Скрипт не запускается:

Убедитесь, что у вас Ubuntu/Debian на VPS:
```bash
lsb_release -a
```

### DNS не обновляется:

Подождите еще. Проверяйте каждые 15 минут:
```bash
nslookup ваш-домен.com
```

### Ошибка при установке SSL:

Убедитесь, что DNS уже обновился и домен указывает на ваш VPS.

### Сайт не открывается:

Проверьте статус:
```bash
pm2 status
systemctl status nginx
pm2 logs invest2026
```

---

## 💡 Совет

Если что-то не работает, скажите на каком шаге возникла проблема, и я помогу!

---

## 🎉 Следующий шаг

**ЗАПУСТИТЕ КОМАНДЫ ИЗ ШАГА 1 И 2 ПРЯМО СЕЙЧАС!**

```bash
ssh root@IP_ВАШЕГО_VPS
wget https://raw.githubusercontent.com/KHUDOYDOD/invest2026/main/vps-setup.sh
bash vps-setup.sh
```

Удачи! 🚀
