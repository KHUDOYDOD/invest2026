# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ ЗАЯВОК

## ✅ Что исправлено

1. **API для создания заявок** - `/api/deposit-requests` (POST)
2. **API для просмотра заявок пользователя** - `/api/user/deposit-requests` (GET)
3. **API для админ-панели** - `/api/admin/deposit-requests` (GET)
4. **API для обработки заявок** - `/api/admin/deposit-requests/[id]` (PATCH)
5. **Форма пополнения** - добавлен токен авторизации
6. **Страница "Мои заявки"** - исправлены запросы
7. **Админ-панель** - исправлены запросы и методы
8. **Роль пользователя** - `salamoni@salamoni.salamoni` теперь admin

## 🚀 Как протестировать

### Шаг 1: Обновить страницу
1. Откройте браузер
2. Нажмите `Ctrl + Shift + R` (жесткое обновление)
3. Или очистите кэш браузера

### Шаг 2: Войти в систему
1. Если вы не авторизованы, войдите как:
   - Email: `salamoni@salamoni.salamoni`
   - Пароль: ваш пароль

### Шаг 3: Проверить "Мои заявки"
1. Откройте страницу "Мои заявки" (в меню слева)
2. Должны отображаться **4 заявки**:
   - $250 - Банковская карта
   - $1000 - Система быстрых платежей
   - $2500 - Система быстрых платежей
   - $500 - Bitcoin
3. Все со статусом "На проверке" (желтый значок)

### Шаг 4: Проверить админ-панель
1. Откройте админ-панель: `http://localhost:3000/admin/requests`
2. Должны отображаться те же **4 заявки**
3. У каждой заявки должны быть кнопки:
   - ✅ Одобрить
   - ❌ Отклонить

### Шаг 5: Одобрить заявку
1. Выберите любую заявку (например, на $250)
2. Нажмите "Одобрить"
3. Должно появиться уведомление "Заявка одобрена"
4. Статус заявки изменится на "Одобрено" (зеленый значок)
5. Проверьте баланс на дашборде - он должен увеличиться на $250

### Шаг 6: Создать новую заявку
1. Откройте дашборд
2. Нажмите кнопку "Пополнить"
3. Введите сумму (например, $100)
4. Выберите способ оплаты
5. Нажмите "Отправить заявку"
6. Должно появиться уведомление "Заявка создана!"
7. Откройте "Мои заявки" - новая заявка должна появиться

## 🔍 Проверка в консоли браузера

Откройте консоль браузера (F12) и проверьте:

### При открытии "Мои заявки":
```
Loading deposit requests for user: 1
Found X deposit requests for user 1
```

### При открытии админ-панели:
```
Admin loading deposit requests...
Found X deposit requests
```

### При создании заявки:
```
🚀 Creating deposit request...
✅ Deposit request created: {id: X, ...}
```

## ❌ Если не работает

### Проблема: "Не авторизован" (401)
**Решение:**
1. Выйдите из системы
2. Войдите снова
3. Проверьте, что в localStorage есть `authToken`:
   - Откройте консоль (F12)
   - Вкладка "Application" → "Local Storage"
   - Должен быть ключ `authToken` с длинным значением

### Проблема: "Доступ запрещен" (403)
**Решение:**
1. Проверьте роль пользователя:
   ```bash
   node check-user-role.js
   ```
2. Если роль не admin, скрипт автоматически обновит её

### Проблема: Заявки не отображаются
**Решение:**
1. Проверьте заявки в базе:
   ```bash
   node check-deposit-requests.js
   ```
2. Проверьте консоль браузера на ошибки
3. Проверьте консоль сервера на ошибки

### Проблема: Сервер не запущен
**Решение:**
```bash
npm run dev
```

## 📊 Текущее состояние

В базе данных есть **4 заявки** на общую сумму **$4250**:
- ID: 1, $500, Bitcoin, pending
- ID: 2, $2500, СБП, pending
- ID: 3, $1000, СБП, pending
- ID: 4, $250, Карта, pending

Все заявки принадлежат пользователю `salamoni@salamoni.salamoni` (ID: 1)

## ✅ Готово!

После выполнения всех шагов система заявок должна работать полностью:
- ✅ Создание заявок
- ✅ Просмотр заявок
- ✅ Одобрение/отклонение
- ✅ Зачисление средств
